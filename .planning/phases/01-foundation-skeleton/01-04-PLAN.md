---
phase: 01-foundation-skeleton
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - crates/prism-types/Cargo.toml
  - crates/prism-types/src/lib.rs
  - crates/prism-types/src/event.rs
  - crates/prism-types/src/service.rs
  - crates/prism-types/src/dto.rs
  - crates/prism-llm/Cargo.toml
  - crates/prism-llm/src/lib.rs
  - crates/prism-llm/src/secrets.rs
  - docs/keychain-naming.md
autonomous: true
requirements: [INFRA-01, INFRA-03]

must_haves:
  truths:
    - "prism-types 为零第三方依赖（仅 serde derive）的纯契约 crate：`cargo tree -p prism-types --edges normal --prefix none | tail -n +2` 只出现 serde 系与 proc-macro 系包"
    - "服务 trait 为同步且 object-safe：`Arc<dyn FeedbackSource>` 与 `Arc<dyn CommentSink>` 可直接构造，prism-types 不依赖 async-trait"
    - "API key 经 keyring-core + apple-native-keyring-store 写入后可读回：`cargo test -p prism-llm roundtrip_with_mock_store` 退出 0"
    - "无 key 时应用照常启动：`get_api_key()` 在无条目时返回 `Ok(None)` 而非 Err——`cargo test -p prism-llm no_key_is_not_an_error` 退出 0"
    - "对同一 (service, account) 连续两次写同一密钥是幂等的：第二次写入后读回值不变、条目仍只有一条；`delete_api_key()` 连续调用两次第二次返回 `Ok(())`（NoEntry 视为已删除）——`cargo test -p prism-llm set_and_delete_are_idempotent` 退出 0"
    - "持有密钥的类型不会经 Debug/日志泄漏原文：`format!(\"{:?}\", ApiKey::new(...))` 的输出不包含密钥原文——`cargo test -p prism-llm apikey_debug_is_redacted` 退出 0"
    - "Keychain 的 service/account 命名是被写下来的跨二进制契约（Phase 6 的 CLI helper 必须用同一套名字），记录于 docs/keychain-naming.md"
    - statement: "prism-llm 的 keyring 测试在默认多线程 harness 下与其他测试并行运行时不互相污染进程级 default store：每个触碰 default store 的测试带 #[serial] 且以 unset_default_store() 收尾，真实钥匙串往返测试带 #[ignore]；`cargo test -p prism-llm` 与 `cargo test --workspace` 各自运行时该 crate 测试均全绿"
      verification: backstop
  artifacts:
    - path: "crates/prism-types/src/service.rs"
      provides: "FeedbackSource / CommentSink 同步 service trait（D-09 的 trait 落点）"
      exports: ["FeedbackSource", "CommentSink", "ServiceError"]
    - path: "crates/prism-types/src/event.rs"
      provides: "EngineEvent 粗粒度事件枚举（含 Resync 变体）"
      contains: "Resync"
    - path: "crates/prism-llm/src/secrets.rs"
      provides: "Keychain 往返：SERVICE/ACCOUNT 常量 + init_default_store + set/get/delete + ApiKey newtype"
      exports: ["SERVICE", "ACCOUNT_LLM_KEY", "ACCOUNT_MCP_TOKEN", "set_api_key", "get_api_key", "delete_api_key"]
      min_lines: 60
    - path: "docs/keychain-naming.md"
      provides: "service/account 命名契约文档（跨 crate 与跨二进制的隐式接口）"
      contains: "mcp_bearer_token"
  key_links:
    - from: "crates/prism-llm/src/secrets.rs"
      to: "apple_native_keyring_store::keychain::Store"
      via: "init_default_store 用 keychain 模块（非 protected）注册进程默认 store"
      pattern: "keychain::Store::new"
    - from: "crates/prism-llm/src/secrets.rs"
      to: "keyring_core::Entry"
      via: "Entry::new(SERVICE, ACCOUNT_LLM_KEY) 的 set/get/delete 往返"
      pattern: "Entry::new\\("
  prohibitions:
    - requirement_id: INFRA-03
      category: privacy
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT 把 API key、MCP bearer token 或任何密钥写入 SQLite（含 settings 表）、仓库内文件、tauri.conf.json、日志或任何会被「数据库单目录整体备份」带走的位置——密钥的唯一存放地是系统钥匙串。"
    - requirement_id: INFRA-03
      category: privacy
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT 在 prism-llm 之外开辟第二条网络出口，也 MUST NOT 在钥匙串不可用或读取失败时静默回退到环境变量、dotfile 或任何明文来源取密钥——失败必须显式冒泡，不得用一条看不见的降级路径把「密钥只在钥匙串」的承诺变成假的。"
---

<objective>
建立两件事：D-09 的 trait 落点（prism-types 零依赖契约 crate），以及 INFRA-03 的唯一密钥入口
（prism-llm 的 Keychain 往返）。

Purpose: prism-types 决定整个 workspace 的依赖方向——Phase 6/7 的 MCP 工具注册都建在它之上；
把 trait 设计成**同步**而非 async 是关键简化：底层 rusqlite 本就是阻塞的，同步 trait 天然
object-safe，`Arc<dyn FeedbackSource>` 直接可用，prism-types 因此可以保持零第三方依赖。
prism-llm 侧则要一次做对 keyring 4 的拆分用法（keyring-core + 平台 store crate + 启动时
`set_default_store`），以及 `keychain` 而非 `protected` 模块——后者在无 provisioning profile 的
直发公证 DMG 上会以 `-34018` 运行期失败。

Output: prism-types 的 trait/事件/DTO 契约、prism-llm 的密钥往返与 redaction、Keychain 命名契约文档。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-skeleton/01-VALIDATION.md
@.planning/phases/01-foundation-skeleton/01-01-SUMMARY.md
@.planning/phases/01-foundation-skeleton/01-02-SUMMARY.md
</context>

> **依赖说明**：本 plan 的验收项调用 `scripts/check-deps.sh` 与 `scripts/check-secrets.sh`
> （由 plan 01-02 创建），因此 `depends_on` 显式列出 `01-02` 并落在其之后的 wave。
> 保留断言在本 plan 里（而不是挪到 wave 闸门）是有意的：`single-egress` 断言必须在
> prism-llm 首次获得 keyring/reqwest 依赖的**同一个 commit** 上执行，否则它守不住那次变更。

## Artifacts this phase produces

本 plan 新建的符号：

- **prism-types 模块**：`event`、`service`、`dto`
- **prism-types 类型**：`EngineEvent`（变体 `DocChanged` / `InboxUpdated` / `Resync`）、
  `FeedbackSource` trait（`list_feedback`）、`CommentSink` trait（`record_receipt`）、
  `ServiceError`、`FeedbackItem`、`Receipt`、`SearchHit`
- **prism-llm 模块**：`secrets`
- **prism-llm 符号**：常量 `SERVICE`（值 `PrismDocs`）、`ACCOUNT_LLM_KEY`（值 `llm_api_key`）、
  `ACCOUNT_MCP_TOKEN`（值 `mcp_bearer_token`）、`init_default_store()`、`set_api_key()`、
  `get_api_key()`、`delete_api_key()`、`ApiKey` newtype（自定义 Debug）、`LlmError`
- **文档**：`docs/keychain-naming.md`
- **测试名**：`roundtrip_with_mock_store`、`no_key_is_not_an_error`、`set_and_delete_are_idempotent`、
  `apikey_debug_is_redacted`、`roundtrip_with_real_keychain`（`#[ignore]`）

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: prism-types 零依赖契约——同步 service trait、EngineEvent、共享 DTO</name>
  <reversibility rating="costly">service trait 的落点（prism-types）与形态（同步而非 async）决定 workspace 的依赖方向与 prism-mcp handler 的调用形态；Phase 6/7 的全部 MCP 工具注册都建在其上，改成 async 需要 prism-types 引入 async-trait 并同步改动 prism-mcp 与 prism-engine 三处。无数据迁移、无已发布契约，故为 costly 而非 one-way（D-09）。</reversibility>
  <files>crates/prism-types/Cargo.toml, crates/prism-types/src/lib.rs, crates/prism-types/src/event.rs, crates/prism-types/src/service.rs, crates/prism-types/src/dto.rs</files>
  <read_first>
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Pattern 4（prism-types 同步 trait 反转）——依赖方向图、trait 定义、`EngineEvent` 载荷契约、为什么是 `dyn` + `Arc` 而不是泛型
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Alternatives Considered（「同步 service trait vs `#[async_trait]`」与「`Arc<dyn Trait>` vs 泛型参数」两行的取舍依据）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Pattern 5（`EngineEvent` 在总线与 shell adapter 两侧的用法，决定它必须 `Clone + Serialize`）
    - `.planning/phases/01-foundation-skeleton/01-CONTEXT.md` § Crate 骨架完整度（D-09）
    - `crates/prism-types/src/lib.rs`（plan 01-01 建立的最小编译单元现状）
  </read_first>
  <behavior>
    - `EngineEvent` 可 `Clone`、可 `serde_json::to_string`，序列化后 `DocChanged` 的字段为 camelCase 的 `projectId` / `docId`，判别字段名为 `kind`
    - `EngineEvent::Resync` 序列化为不带额外字段的对象
    - `Arc<dyn FeedbackSource>` 与 `Arc<dyn CommentSink>` 可由一个测试用假实现构造并调用（证明 object-safe）
    - `ServiceError` 实现 `std::error::Error` 且其 Display 不包含任何调用参数原文
  </behavior>
  <action>
    把 prism-types 从 plan 01-01 的最小编译单元扩展为真正的契约 crate，拆成三个模块保持文件短小。

    `Cargo.toml`：依赖**只有** `serde`（`features = ["derive"]`）与 `thiserror`。
    **不得**引入 `async-trait`、`tokio`、`anyhow` 或任何 engine crate——prism-types 是依赖图的汇点，
    prism-mcp 与 prism-engine 都指向它，任何新增依赖都会同时压到两侧。

    `src/event.rs`：`pub enum EngineEvent`，`#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]`
    加 `#[serde(tag = "kind", rename_all = "camelCase")]`。三个变体：
    `DocChanged { project_id: String, doc_id: String }`、`InboxUpdated { project_id: String, unread: u32 }`、
    `Resync`。这是**粗粒度失效信号**的载荷契约——只带 ID 与计数，不带文档正文；
    `Resync` 专供总线 `Lagged` 时的全量失效使用（plan 01-08 消费）。
    载荷保持小是 notify-then-fetch 相对 push-payload 的全部价值所在。

    `src/service.rs`：两个**同步**（非 async）trait，各自 `: Send + Sync + 'static`：
    `pub trait FeedbackSource` 含 `fn list_feedback(&self, project_id: &str) -> Result<Vec<FeedbackItem>, ServiceError>`；
    `pub trait CommentSink` 含 `fn record_receipt(&self, receipt: Receipt) -> Result<(), ServiceError>`。
    同步是刻意选择：底层 prism-store 基于 rusqlite 本就是阻塞的，rmcp handler 在自己的 async 上下文里
    用 `tokio::task::spawn_blocking` 调用即可；同步 trait 天然 object-safe，不触碰 AFIT 的 dyn-safety 问题，
    也让 prism-types 不需要 `async-trait` 依赖。
    `ServiceError` 用 thiserror 定义，变体覆盖 `NotFound`、`Backend(String)`、`Invalid(String)`；
    Display 文本**不得回显调用方传入的原始参数**（避免经 MCP 响应把内部标识回抛）。
    模块顶部写一行注释说明：Phase 6/7 新增工具时在此追加 trait，不动 prism-mcp。

    `src/dto.rs`：`FeedbackItem`、`Receipt`、`SearchHit { doc_id: String, title: Option<String>, rel_path: String }`，
    全部 `#[derive(Clone, Debug, Serialize, Deserialize)]` + `#[serde(rename_all = "camelCase")]`。
    Phase 1 的 `FeedbackItem` / `Receipt` 只需承载 plan 01-06 假数据 handler 所需的最小字段
    （`id`、`doc_id`、`body`；`comment_id`、`status`），字段扩充留给 Phase 6。

    `src/lib.rs` 改为模块聚合器并 re-export 上述公开项，保留 `CRATE_VERSION`。
    同文件 `#[cfg(test)] mod tests` 里写一个假实现（如 `struct FakeSource;`）并把它装进
    `Arc<dyn FeedbackSource>` 调一次——这是 object-safety 的编译期证明。
  </action>
  <verify>
    <automated>cargo test -p prism-types</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p prism-types` 退出 0
    - `cargo tree -p prism-types --edges normal --prefix none | tail -n +2 | grep -cvE '^(serde|serde_derive|serde_json|thiserror|thiserror-impl|proc-macro2|quote|syn|unicode-ident)( |$)'` 输出 0
    - `crates/prism-types/src/service.rs` 中 `async fn` 出现 0 次，`async_trait` 出现 0 次
    - `crates/prism-types/Cargo.toml` 的 `[dependencies]` 段只含 serde 与 thiserror 两项
    - 单测断言：`serde_json::to_string(&EngineEvent::DocChanged{..})` 的结果同时包含 `"kind"`、`"projectId"`、`"docId"`
    - 单测断言：`serde_json::to_string(&EngineEvent::Resync)` 的结果包含 `"kind"` 且不包含 `projectId`
    - 单测（编译期证明）：把一个假实现装进 `Arc<dyn FeedbackSource>` 并调用 `list_feedback` 返回 Ok
    - `cargo clippy -p prism-types -- -D warnings` 退出 0
    - `bash scripts/check-deps.sh single-egress` 与 `bash scripts/check-deps.sh tauri-free` 仍退出 0
  </acceptance_criteria>
  <done>prism-types 成为零第三方依赖（仅 serde/thiserror）的契约 crate；两个同步 object-safe trait、EngineEvent（含 Resync）与三个 DTO 就位并有编译期 object-safety 证明。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: prism-llm 密钥入口——Keychain 往返、redaction 与命名契约</name>
  <files>crates/prism-llm/Cargo.toml, crates/prism-llm/src/lib.rs, crates/prism-llm/src/secrets.rs, docs/keychain-naming.md</files>
  <read_first>
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Code Examples → keyring 往返（真实 Keychain / mock 两条路径）——常量、`init_default_store`、`NoEntry → Ok(None)`、mock 与 `#[ignore]` 两组测试的完整形态
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Common Pitfalls → Pitfall 1（`set_default_store` 是进程全局，与并行测试冲突；两种解法）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Common Pitfalls → Pitfall 2（`keychain` vs `protected` 模块与 `-34018`）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Runtime State Inventory（service/account 命名是跨二进制的隐式接口，必须在 Phase 1 写进 docs/）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Security Domain → V6 / V7（密钥存储全权交给 Keychain，绝不自建加密存储；tracing 输出禁止出现密钥）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Open Questions → 第 2 条（`cargo-nextest` vs `serial_test` 的取舍）
    - `crates/prism-llm/src/lib.rs`（plan 01-01 建立的 `user_agent()` 最小编译单元现状）
  </read_first>
  <behavior>
    - `roundtrip_with_mock_store`：装上 `keyring_core::mock::Store` 后，初始 `get_api_key()` 为 `Ok(None)`；
      `set_api_key(v)` 后 `get_api_key()` 为 `Ok(Some(v))`；`delete_api_key()` 后回到 `Ok(None)`
    - `no_key_is_not_an_error`：无条目时 `get_api_key()` 返回 `Ok(None)`，**不是** Err（D-06：无 key 应用照常启动）
    - `set_and_delete_are_idempotent`：连续两次 `set_api_key` 写同一值后读回值不变；连续两次
      `delete_api_key` 第二次仍返回 `Ok(())`；删除后再删除不产生 Err
    - `apikey_debug_is_redacted`：`format!("{:?}", ApiKey::new("some-secret-value"))` 的输出不含
      `some-secret-value` 子串
    - `roundtrip_with_real_keychain`（`#[ignore]`）：注册真实 keychain store 后完成一次 set/get/delete 往返
    - 上述每个触碰进程默认 store 的测试都带 `#[serial]` 且以 `unset_default_store()` 收尾
  </behavior>
  <action>
    实现 INFRA-03 的唯一密钥入口。**prism-llm 是全 workspace 中唯一被允许持有 keyring 与 reqwest
    依赖的 engine crate**（plan 01-02 的 `single-egress` 断言强制这一点）。

    `Cargo.toml` 补齐：`keyring-core`、`apple-native-keyring-store`（**必须带 `features = ["keychain"]`**）、
    `reqwest`、`thiserror`、`serde`；`[dev-dependencies]` 加 `serial_test`。
    **禁用** `tauri-plugin-stronghold` 与 `tauri-plugin-keyring`（CLAUDE.md What-NOT-to-use）。

    `src/secrets.rs`：
    - 三个公开常量：`SERVICE = "PrismDocs"`、`ACCOUNT_LLM_KEY = "llm_api_key"`、
      `ACCOUNT_MCP_TOKEN = "mcp_bearer_token"`。这三个字符串是**跨二进制契约**——Phase 6 的
      `prismdocs-helper` 必须用同一套名字读同一条目，写错要到 Phase 6 才暴露。
    - `#[cfg(target_os = "macos")] pub fn init_default_store() -> Result<(), LlmError>`：调
      `keyring_core::set_default_store(apple_native_keyring_store::keychain::Store::new()?)`。
      **必须用 `keychain` 模块而不是 `protected`**：PrismDocs 是直发公证 DMG、不进 App Sandbox、
      无 provisioning profile，用 `protected` 会在运行期报 `-34018 A required entitlement isn't present`。
      文档注释注明「应用启动时调用一次，且必须在创建任何 Entry 之前」。
    - `set_api_key(secret: &str) -> Result<(), LlmError>`：`Entry::new(SERVICE, ACCOUNT_LLM_KEY)?.set_password(secret)`。
    - `get_api_key() -> Result<Option<String>, LlmError>`：match `get_password()`，`Ok(s) => Ok(Some(s))`，
      `Err(keyring_core::Error::NoEntry) => Ok(None)`，其余冒泡。注意 `keyring_core::Error` 是
      `#[non_exhaustive]`，match 必须带 `_` 臂。**不得**在任何错误分支上回退去读环境变量或明文文件。
    - `delete_api_key() -> Result<(), LlmError>`：`NoEntry` 视为已删除返回 `Ok(())`（幂等）。
    - `pub struct ApiKey(String)` newtype，`ApiKey::new` / `ApiKey::expose`，
      **手写 `impl std::fmt::Debug`** 输出固定的 redacted 占位串而非内容（Security Domain V7：
      `tracing` 输出中禁止出现密钥）。`LlmError` 的任何变体都不得携带密钥原文。

    测试按 `<behavior>` 写全，放在 `src/secrets.rs` 的 `#[cfg(test)] mod tests`。
    并行测试冲突用 **`serial_test` 方案**（RESEARCH Open Question 2 的方案 b）——它零工具链要求，
    与 `01-VALIDATION.md` 记录的纯 `cargo test` 命令一致；`cargo-nextest` 作为可选加强手段写进
    docs 但不作为必需依赖。每个触碰默认 store 的测试标 `#[serial]` 并以 `unset_default_store()` 收尾。
    真实钥匙串往返测试必须标 `#[ignore]` 并在 reason 里写明手动运行方式——dev 期签名身份变化会反复
    触发 Keychain 授权弹窗，且 CI/headless 无解锁钥匙串。

    `src/lib.rs` 改为 `pub mod secrets;` 聚合并保留 `user_agent()`。

    `docs/keychain-naming.md` 记录命名契约：service 名、两个 account 名、各自用途、写入方（prism-llm）、
    读取方（prism-llm 与 Phase 6 的 prismdocs-helper）、以及「更名等于让已安装用户的密钥不可见」的警示。
  </action>
  <verify>
    <automated>cargo test -p prism-llm</automated>
    <human-check>`cargo test -p prism-llm -- --ignored roundtrip_with_real_keychain` 手动运行一次，系统钥匙串授权弹窗出现后允许，测试通过；随后在「钥匙串访问」中确认 `PrismDocs` 条目已被清理</human-check>
  </verify>
  <acceptance_criteria>
    - `cargo test -p prism-llm` 退出 0，且 `roundtrip_with_mock_store`、`no_key_is_not_an_error`、
      `set_and_delete_are_idempotent`、`apikey_debug_is_redacted` 四个测试均执行并通过
    - `cargo test -p prism-llm` 的输出中显示至少 1 个 ignored 测试（真实钥匙串路径未被自动执行）
    - `crates/prism-llm/Cargo.toml` 中 `apple-native-keyring-store` 一行包含 `features = ["keychain"]`
    - `crates/prism-llm/src/secrets.rs` 中 `keychain::Store::new` 出现 ≥1 次，`protected` 出现 0 次
    - `crates/prism-llm/src/secrets.rs` 中 `impl std::fmt::Debug for ApiKey` 存在（非 `#[derive(Debug)]`）
    - `crates/prism-llm/src/secrets.rs` 中 `std::env::var` 与 `dotenv` 均出现 0 次（无明文回退路径）
    - `grep -v '^//' crates/prism-llm/src/secrets.rs | grep -c 'unset_default_store'` 输出 ≥ 带 `#[serial]` 的测试个数
    - `cargo test -p prism-llm` 连续运行 3 次均退出 0（无 flake）
    - `docs/keychain-naming.md` 同时包含 `PrismDocs`、`llm_api_key`、`mcp_bearer_token` 三个字符串
    - `bash scripts/check-secrets.sh` 退出 0（测试用的假密钥不构成明文密钥命中）
    - `cargo clippy -p prism-llm -- -D warnings` 退出 0
  </acceptance_criteria>
  <done>API key 可经系统钥匙串写入并读回、无 key 时返回 Ok(None)、写删均幂等、Debug 不泄漏原文；service/account 命名契约落成文档；真实钥匙串测试以 #[ignore] 留给人工。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine(prism-llm) → macOS 系统钥匙串 | 唯一密钥入口；此处形态错误（模块选错、回退路径）即让「密钥只在钥匙串」的承诺失效 |
| engine(prism-llm) → 用户配置的 LLM 端点 | 唯一网络出口；Phase 1 只建 crate 与 UA，实际请求在 Phase 4 |
| prism-mcp / prism-engine → prism-types | 契约汇点；此 crate 的依赖面同时压在两侧 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Information Disclosure | API key 经 `Debug` / `tracing` / 错误信息泄漏 | high | mitigate | `ApiKey` newtype 手写 Debug 输出 redacted 占位；`LlmError` 变体不携带密钥原文；`apikey_debug_is_redacted` 测试锁死 |
| T-01-21 | Information Disclosure | 钥匙串失败时静默回退到环境变量 / dotfile 取密钥 | high | mitigate | `get_api_key` 只把 `NoEntry` 映射为 `Ok(None)`，其余错误一律冒泡；验收项断言源码中 `std::env::var` 与 `dotenv` 出现 0 次 |
| T-01-22 | Spoofing / Denial of Service | 选用 `protected` 模块导致运行期 `-34018`，或 service/account 命名与 Phase 6 CLI helper 不一致 | medium | mitigate | 强制 `features = ["keychain"]` + `keychain::Store::new`；命名固化进 `docs/keychain-naming.md` 并被验收项断言 |
| T-01-23 | Tampering | 测试之间经进程级 `set_default_store` 互相污染，掩盖真实失败 | medium | mitigate | `#[serial]` + `unset_default_store()` 收尾；真实钥匙串测试 `#[ignore]`；连续 3 次 `cargo test -p prism-llm` 全绿作为弱证据（该项在 must_haves 中登记为 backstop，验证不足时交人工判定） |
| T-01-24 | Elevation of Privilege | prism-types 引入第三方依赖后同时污染 prism-mcp 与 prism-engine 两侧 | low | accept | 依赖面由验收项断言（只允许 serde/thiserror 系）；残余风险为传递依赖的次要版本漂移，由 `Cargo.lock` 与 plan 01-02 的 dup 断言兜底 |
</threat_model>

<verification>
- `cargo test -p prism-types -p prism-llm` 全绿
- `cargo clippy -p prism-types -p prism-llm -- -D warnings` 退出 0
- `bash scripts/check-deps.sh single-egress` 退出 0（prism-llm 之外无第二条出口）
- `bash scripts/check-secrets.sh` 退出 0
- 人工：真实钥匙串往返 `cargo test -p prism-llm -- --ignored` 通过
</verification>

<success_criteria>
成功标准 4 的引擎侧：API key 经 keyring-core + apple-native-keyring-store 写入系统钥匙串并可读回，
prism-llm 为唯一网络出口与唯一密钥入口，代码与配置中无明文密钥；
以及成功标准 1 的 trait 反转前置条件（prism-types 契约就位）。
</success_criteria>

<output>
Create `.planning/phases/01-foundation-skeleton/01-04-SUMMARY.md` when done
</output>
