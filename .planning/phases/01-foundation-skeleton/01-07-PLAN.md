---
phase: 01-foundation-skeleton
plan: 07
type: execute
wave: 5
depends_on: ["01-04", "01-05", "01-06"]
files_modified:
  - crates/prism-engine/Cargo.toml
  - crates/prism-engine/src/lib.rs
  - crates/prism-engine/src/bus.rs
  - crates/prism-engine/src/facade.rs
  - crates/prism-engine/src/services.rs
  - crates/prism-engine/src/error.rs
  - crates/prism-engine/tests/facade.rs
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03]

must_haves:
  truths:
    - "prism-engine 是唯一的编排入口且不依赖 tauri：`bash scripts/check-deps.sh tauri-free` 退出 0，`cargo test -p prism-engine` 在不编译 tauri 的情况下全绿"
    - "engine 持有单写者句柄，所有写路径经它：facade 不暴露任何返回原始 Connection 或池连接的公开方法"
    - "订阅者可收到 publish 的事件：`cargo test -p prism-engine bus_delivers_to_subscriber` 退出 0"
    - "无订阅者时 publish 不 panic、不返回错误给调用方：`cargo test -p prism-engine publish_with_no_subscriber_is_ok` 退出 0（broadcast 在无接收端时 send 返回 Err，engine 必须吞掉它——这是空输入的正确语义）"
    - "多个订阅者各自独立收到同一条事件（广播语义，非竞争消费）"
    - "engine 实现 prism-types 的 FeedbackSource / CommentSink，可被装进 `Arc<dyn …>` 注入 prism-mcp：`cargo test -p prism-engine engine_satisfies_service_traits` 退出 0"
    - "engine 的搜索/设置/密钥三个门面方法各自委托到正确的下层：搜索经 prism-store、密钥经 prism-llm、非密钥配置经 prism-store settings 表，且 engine 自身不直接持有 keyring 或 reqwest 依赖"
  artifacts:
    - path: "crates/prism-engine/src/bus.rs"
      provides: "tokio broadcast 事件总线（subscribe / publish）"
      exports: ["EventBus"]
    - path: "crates/prism-engine/src/facade.rs"
      provides: "Engine 门面：ping / search / settings / api_key 的编排与 spawn_blocking 包装"
      exports: ["Engine"]
      min_lines: 70
    - path: "crates/prism-engine/src/services.rs"
      provides: "impl FeedbackSource for Engine / impl CommentSink for Engine（D-09 注入面）"
      contains: "impl FeedbackSource"
    - path: "crates/prism-engine/tests/facade.rs"
      provides: "总线交付、无订阅者、多订阅者、trait 实现、门面委托五组集成测试"
      min_lines: 70
  key_links:
    - from: "crates/prism-engine/src/facade.rs"
      to: "crates/prism-store/src/open.rs"
      via: "Engine 持有 Arc<Store>，写路径统一经 store.write 闭包"
      pattern: "\\.write\\(\\|"
    - from: "crates/prism-engine/src/facade.rs"
      to: "crates/prism-llm/src/secrets.rs"
      via: "密钥读写唯一经 prism_llm::secrets，engine 不直接触碰 keyring"
      pattern: "prism_llm::secrets"
    - from: "crates/prism-engine/src/services.rs"
      to: "prism_types::FeedbackSource"
      via: "Engine 实现服务 trait，供 prism-mcp 以 Arc<dyn> 注入"
      pattern: "impl FeedbackSource for Engine"
  prohibitions: []
---

<objective>
建立 prism-engine 门面：持有 Store 句柄与事件总线，实现 prism-types 的服务 trait，
把 store / llm / mcp 三侧编排起来，且全程不依赖 tauri。

Purpose: 这是 D-01 的支点——shell 只是薄壳，所有编排在这里，因此这一层必须能在没有 tauri 的情况下
被完整测试。同时它是单写者纪律的持有者（Phase 2+ 所有写路径必须经它，不允许「随手拿池连接写」），
以及事件总线的唯一订阅点（Phase 2 的 watcher 事件直接接进来）。

Output: `EventBus`、`Engine` 门面、`impl FeedbackSource/CommentSink for Engine`、集成测试。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-skeleton/01-VALIDATION.md
@.planning/phases/01-foundation-skeleton/01-04-SUMMARY.md
@.planning/phases/01-foundation-skeleton/01-05-SUMMARY.md
@.planning/phases/01-foundation-skeleton/01-06-SUMMARY.md
</context>

## Artifacts this phase produces

本 plan 新建的符号：

- **模块**：`prism_engine::bus`、`prism_engine::facade`、`prism_engine::services`、`prism_engine::error`
- **类型与函数**：`EventBus`、`EventBus::new`、`EventBus::subscribe`、`EventBus::publish`、
  `Engine`（扩展 plan 01-01 的 tracer 版）、`Engine::new`、`Engine::ping`、`Engine::subscribe`、
  `Engine::publish`、`Engine::search`、`Engine::get_setting`、`Engine::set_base_url`、
  `Engine::set_api_key`、`Engine::api_key_status`、`Engine::init_secrets`、`EngineError`
- **常量**：`BUS_CAPACITY`（broadcast 环形缓冲容量）
- **trait 实现**：`impl FeedbackSource for Engine`、`impl CommentSink for Engine`
- **测试名**：`bus_delivers_to_subscriber`、`publish_with_no_subscriber_is_ok`、
  `bus_broadcasts_to_multiple_subscribers`、`engine_satisfies_service_traits`、
  `search_delegates_to_store`、`base_url_goes_to_settings_not_keychain`

从兄弟 plan 消费的符号：`prism_store::{Store, open, search, settings}`（plan 01-03 / 01-05）、
`prism_types::{EngineEvent, FeedbackSource, CommentSink, SearchHit}`（plan 01-04）、
`prism_llm::secrets::*`（plan 01-04）、`prism_mcp::{McpDeps, serve_loopback}`（plan 01-06）。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EventBus（tokio broadcast）与 Engine 门面编排</name>
  <files>crates/prism-engine/Cargo.toml, crates/prism-engine/src/lib.rs, crates/prism-engine/src/bus.rs, crates/prism-engine/src/facade.rs, crates/prism-engine/src/error.rs</files>
  <read_first>
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Pattern 5（notify-then-fetch）——engine 侧 `broadcast::Sender<EngineEvent>` 的形态，以及「载荷是粗粒度失效信号」这一设计前提
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Pattern 2（闭包式 write/read API 与 `spawn_blocking` 的分工：阻塞 store + facade 侧 spawn_blocking）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Architectural Responsibility Map（事件总线归 prism-engine；密钥读写归 prism-llm；非密钥配置归 prism-store settings 表）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Anti-Patterns to Avoid（「在 `.await` 上持有 writer 锁」——由 `std::sync::Mutex` 的 !Send guard 在编译期挡住）
    - `.planning/phases/01-foundation-skeleton/01-CONTEXT.md` § Existing Code Insights → Integration Points（单写者句柄由 facade 持有，Phase 2+ 所有写路径必须经它）
    - `crates/prism-store/src/lib.rs` 与 `crates/prism-store/src/open.rs`（plan 01-03/01-05 建立的 `Store::write`/`read`/`search`/`settings` 接口）
    - `crates/prism-llm/src/secrets.rs`（plan 01-04 的密钥接口）
    - `crates/prism-engine/src/lib.rs`（plan 01-01 建立的 tracer 版 Engine 现状）
  </read_first>
  <behavior>
    - `bus_delivers_to_subscriber`：`subscribe()` 后 `publish(EngineEvent::DocChanged{..})`，接收端 `recv().await` 拿到同一事件
    - `publish_with_no_subscriber_is_ok`：没有任何订阅者时 `publish()` 返回单元值、不 panic、不把 broadcast 的 `SendError` 冒泡给调用方
    - `bus_broadcasts_to_multiple_subscribers`：两个独立订阅者各自都收到同一条事件（广播而非竞争消费）
    - `search_delegates_to_store`：向 store 写入一篇含中文标题的文档后，`Engine::search` 返回该文档的 `SearchHit`
    - `base_url_goes_to_settings_not_keychain`：`Engine::set_base_url` 之后，`get_setting` 能读回该值；
      同时 mock keyring store 中不存在任何新增条目（证明非密钥配置没有误走钥匙串）
  </behavior>
  <action>
    把 plan 01-01 的 tracer 版 Engine 扩展为完整门面，拆成三个模块保持文件短小。

    `Cargo.toml`：依赖 `prism-store`、`prism-types`、`prism-llm`、`prism-mcp`、`tokio`
    （`rt-multi-thread` + `sync` + `macros`）、`thiserror`、`tracing`；`[dev-dependencies]` 加
    `tempfile`、`serial_test`、`tokio`（测试宏）。**不得依赖 tauri**——plan 01-02 的 `tauri-free`
    断言覆盖本 crate。注意 prism-engine 依赖 prism-mcp（它构造并托管 server），方向是单向的。

    `src/bus.rs`：`pub struct EventBus(tokio::sync::broadcast::Sender<EngineEvent>)`，
    `new()` 用常量 `BUS_CAPACITY`（取 256——足够吸收一次 FS 批量变更，又不至于让慢接收端长期滞留）
    构造；`subscribe(&self) -> broadcast::Receiver<EngineEvent>`；
    `publish(&self, ev: EngineEvent)` **返回单元值并内部吞掉 `SendError`**——broadcast 在无接收端时
    `send` 返回 Err，但对粗粒度失效信号而言「没人在听」不是错误，把它冒泡给调用方会让每个写路径
    都要处理一个无意义的错误分支。吞掉时用 `tracing::trace!` 记一行。

    `src/facade.rs`：`pub struct Engine { store: Arc<Store>, bus: EventBus }`。
    - `Engine::new(store: Arc<Store>) -> Engine`。
    - `subscribe()` / `publish()` 转发给 bus。
    - `ping()` 保留 plan 01-01 的行为（委托 `store.sqlite_version()`）。
    - `search(&self, project_id: &str, q: &str) -> Result<Vec<SearchHit>, EngineError>`：
      `store.read(|c| prism_store::search(c, project_id, q))`。
    - `get_setting(&self, key: &str)` / `set_base_url(&self, raw: &str)`：后者先过
      `prism_store::settings::validate_base_url` 再经 `store.write(|tx| set_setting(tx, SETTING_BASE_URL, ..))`
      写入，成功后 `publish` 一条事件。
    - `init_secrets()` / `set_api_key(&self, secret: &str)` / `api_key_status(&self) -> Result<bool, EngineError>`：
      **全部委托 `prism_llm::secrets`**，engine 自己不 `use keyring_core`（NFR-03：唯一密钥入口）。
      `api_key_status` 返回布尔而非密钥本身——门面不向上层返回密钥原文。
    - 所有阻塞调用（store 与 keyring）若从 async 上下文进入，由**调用方**（shell 命令层）用
      `spawn_blocking` 包装；facade 的方法本身保持同步，这样 `std::sync::MutexGuard` 的 `!Send`
      仍能在编译期挡住「跨 await 持写锁」。在模块顶部写一行注释说明这条纪律。

    `src/error.rs`：`EngineError`（thiserror），`#[from]` 收 `StoreError` 与 `LlmError`。
    Display 文本**不得**包含数据库路径、密钥原文或用户文档内容——它会经 shell 命令层回传前端。

    `src/lib.rs` 改为模块聚合器并 re-export `Engine`、`EventBus`、`EngineError`。
  </action>
  <verify>
    <automated>cargo test -p prism-engine</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p prism-engine` 退出 0，五个 `<behavior>` 测试全部执行
    - `cargo test -p prism-engine publish_with_no_subscriber_is_ok` 单独运行退出 0
    - `cargo test -p prism-engine bus_broadcasts_to_multiple_subscribers` 单独运行退出 0
    - `cargo test -p prism-engine` 的输出中不出现 `Compiling tauri v`
    - `crates/prism-engine/src/facade.rs` 中 `prism_llm::secrets` 出现 ≥1 次，`keyring_core` 出现 0 次
    - `crates/prism-engine/src/facade.rs` 中不存在返回 `rusqlite::Connection`、`&Connection` 或
      `PooledConnection` 的公开方法（单写者句柄不外泄）
    - `crates/prism-engine/src/bus.rs` 中 `publish` 的返回类型为 `()`（无订阅者不构成调用方错误）
    - `bash scripts/check-deps.sh tauri-free` 与 `bash scripts/check-deps.sh single-egress` 退出 0
    - `cargo clippy -p prism-engine -- -D warnings` 退出 0
  </acceptance_criteria>
  <done>EventBus 广播语义与无订阅者容错落地；Engine 门面把搜索、设置、密钥三条路径分别委托到 store / store settings / prism-llm，且不依赖 tauri、不外泄连接句柄、不直接触碰 keyring。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine 实现 prism-types 服务 trait 并可注入 prism-mcp</name>
  <files>crates/prism-engine/src/services.rs, crates/prism-engine/src/lib.rs, crates/prism-engine/tests/facade.rs</files>
  <read_first>
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Pattern 4（依赖方向图：prism-engine → prism-mcp → prism-types，engine 实现 trait 并以 `Arc<dyn>` 注入）
    - `crates/prism-types/src/service.rs`（plan 01-04 的 trait 签名与 `ServiceError` 变体）
    - `crates/prism-mcp/src/deps.rs` 与 `crates/prism-mcp/src/server.rs`（plan 01-06 的 `McpDeps` 与 `serve_loopback` 签名）
    - `crates/prism-engine/src/facade.rs`（Task 1 建立的 Engine 现状）
  </read_first>
  <behavior>
    - `engine_satisfies_service_traits`：把 `Arc<Engine>` 装进 `Arc<dyn FeedbackSource>` 与
      `Arc<dyn CommentSink>`，构造 `McpDeps`，起 `serve_loopback`，发一次全合法请求，
      响应中出现 engine 返回的数据——这条链路证明 D-09 的注入面在真实类型上成立，而不只在假实现上成立
    - `list_feedback` 在 Phase 1 返回空 vec（评论表属于 Phase 5），且返回 `Ok` 而非 `Err`——
      「还没有反馈」是正常状态不是错误
    - `record_receipt` 在 Phase 1 返回 `Ok(())` 并记一条 `tracing` 日志（真实落库属于 Phase 6）
    - 所有 trait 方法为同步实现，不含 `.await`
  </behavior>
  <action>
    新建 `crates/prism-engine/src/services.rs`，为 `Engine` 实现 prism-types 的两个服务 trait。
    这是 D-09 依赖倒置的落地点：prism-mcp 只认 trait，具体实现在 engine 侧，因此 Phase 6/7 注册
    新工具时只需在 prism-types 加 trait、在 engine 加 impl，**不动 prism-mcp**。

    - `impl FeedbackSource for Engine`：`list_feedback(&self, project_id: &str) -> Result<Vec<FeedbackItem>, ServiceError>`。
      Phase 1 的 schema v1 尚无 comments 表（D-04 的最小集边界），因此返回 `Ok(vec![])`。
      **返回空集合而不是 `Err(NotFound)`**——「这个项目还没有待处理反馈」是正常状态。
      实现里可以顺带校验 `project_id` 非空并在为空时返回 `ServiceError::Invalid`，
      为 Phase 6 的真实实现留好边界校验位置。
    - `impl CommentSink for Engine`：`record_receipt` 记一条 `tracing::info!`（只记 receipt 的 ID 与
      状态，**不记正文**）后返回 `Ok(())`。
    - 两个 impl 都是同步的（trait 本身同步），因此实现体内不得出现 `.await`；若未来需要阻塞 IO，
      调用方 prism-mcp 的 handler 已经用 `spawn_blocking` 包好了。

    `tests/facade.rs`：把 Task 1 的五个测试与本 task 的 `engine_satisfies_service_traits` 合在一个
    集成测试文件里（Task 1 的单测可留在各自模块内，集成层只放跨 crate 的那几条）。
    `engine_satisfies_service_traits` 必须走完整链路：真实 `Engine` → `Arc<dyn FeedbackSource>` →
    `McpDeps` → `serve_loopback` → HTTP 请求 → 断言响应。这是本阶段成功标准 1 第三条的端到端证据。
    测试用 `tempfile::TempDir` 注入数据根，keyring 相关路径用 mock store 并标 `#[serial]`。
  </action>
  <verify>
    <automated>cargo test -p prism-engine --test facade &amp;&amp; cargo test -p prism-types -p prism-store -p prism-fs -p prism-parse -p prism-anchor -p prism-llm -p prism-mcp -p prism-engine</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p prism-engine --test facade engine_satisfies_service_traits` 退出 0
    - engine 选择集测试 `cargo test -p prism-types -p prism-store -p prism-fs -p prism-parse -p prism-anchor -p prism-llm -p prism-mcp -p prism-engine` 退出 0
      且输出中不出现 `Compiling tauri v`（这是 D-01 的唯一合法证据形态）
    - `bash scripts/check-deps.sh all` 退出 0
    - `crates/prism-engine/src/services.rs` 中 `impl FeedbackSource for Engine` 与
      `impl CommentSink for Engine` 各出现 1 次，`.await` 出现 0 次
    - 单测断言：`list_feedback("p1")` 返回 `Ok` 且长度为 0；`list_feedback("")` 返回 `Err`
    - 单测断言：`record_receipt` 返回 `Ok(())`
    - `cargo clippy -p prism-engine -- -D warnings` 退出 0
  </acceptance_criteria>
  <done>Engine 实现两个服务 trait 并已被证明可装进 `Arc<dyn …>` 注入真实的 prism-mcp server 完成一次请求；engine 选择集测试全绿且全程不编译 tauri。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri shell → prism-engine | shell 的命令载荷（前端传来的字符串）在此进入编排层 |
| prism-engine → prism-store / prism-llm / prism-mcp | 单写者句柄、密钥入口与 MCP 注入面的汇聚点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-31 | Elevation of Privilege | facade 向上层泄漏原始 `Connection` / 池连接，使调用方绕过单写者纪律 | high | mitigate | Engine 不暴露任何返回连接类型的公开方法（验收项断言）；写路径统一经 `store.write(\|tx\| …)` 闭包 |
| T-01-04a | Information Disclosure | `api_key_status` 之类的门面方法把密钥原文回传上层 | high | mitigate | 状态查询返回布尔而非密钥；`EngineError` 不携带密钥原文；密钥读写唯一经 `prism_llm::secrets`（验收项断言 engine 源码不出现 `keyring_core`） |
| T-01-20a | Information Disclosure | `EngineError` 的 Display 携带数据库绝对路径或用户文档片段，经命令层回传前端 | medium | mitigate | 错误变体只保留类别与下层错误码；plan 01-08 的命令层再做一次映射 |
| T-01-32 | Denial of Service | 慢订阅者导致 broadcast 环形缓冲塞满，publish 端被拖慢 | low | accept | broadcast 的语义是丢弃最旧消息而非阻塞发送端，且载荷是粗粒度失效信号（丢弃可由一次 Resync 无损补偿，见 plan 01-08）；`BUS_CAPACITY` 取 256。残余风险可接受 |
| T-01-33 | Information Disclosure | `record_receipt` 把回执正文写进日志 | medium | mitigate | 只记 receipt 的 ID 与状态，不记正文 |
</threat_model>

<verification>
- `cargo test -p prism-engine` 全绿
- engine 选择集 `cargo test -p prism-types -p prism-store -p prism-fs -p prism-parse -p prism-anchor -p prism-llm -p prism-mcp -p prism-engine` 全绿且不编译 tauri（成功标准 1 第一条）
- `cargo clippy -p prism-engine -- -D warnings` 退出 0
- `bash scripts/check-deps.sh all` 退出 0
</verification>

<success_criteria>
成功标准 1 的第一条与第三条在 engine 侧全部兑现：workspace 不依赖 tauri 即可 `cargo test` 全绿，
prism-mcp 经注入的真实 Engine 完成一次端到端请求。
</success_criteria>

<output>
Create `.planning/phases/01-foundation-skeleton/01-07-SUMMARY.md` when done
</output>
