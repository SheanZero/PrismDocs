---
phase: 01-foundation-skeleton
plan: 26
type: execute
wave: 4
depends_on: ["01-22", "01-23", "01-24"]
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.js
  - .planning/phases/01-foundation-skeleton/01-RESEARCH.md
autonomous: false
gap_closure: true
requirements: [INFRA-01]
tags: [tooling, linting, frontend, package-legitimacy]

must_haves:
  truths:
    - "`npm run lint` 存在且在当前代码库上退出 0"
    - "把一次条件分支里的 hook 调用、一条缺失依赖的 `useEffect`、一条未 await 的 promise 分别写进任一 `src/` 文件后，`npm run lint` 退出 1 并点名对应规则（`react-hooks/rules-of-hooks` / `react-hooks/exhaustive-deps` / `@typescript-eslint/no-floating-promises`）"
    - "所有新增 npm 包在安装**之前**经过人工合法性核对，且核对结论写回 `01-RESEARCH.md` § Package Legitimacy Audit"
    - "`npm run build`（含 `tsc --noEmit`）与 `npm run test -- --run` 仍全绿"
    - statement: "新增的 lint 规则集不得要求改动本轮其他 plan 刚落地的实现语义；若某条规则与既有代码冲突，优先调整规则配置并写明理由，不得为了让 lint 过而削弱任何安全断言"
      verification: backstop
  artifacts:
    - "eslint.config.js —— flat config，覆盖 `src/**/*.{ts,tsx}`"
    - "package.json —— `lint` script 与新增 devDependencies"
    - ".planning/phases/01-foundation-skeleton/01-RESEARCH.md —— Package Legitimacy Audit 表追加本轮新包"
  key_links:
    - "`npm run lint` → `.github/workflows/ci.yml` 的 frontend job（CI 接线由 01-27 完成，本 plan 只提供 script）"
    - "lint 规则 → `src/lib/ipc.ts` 的 `errorCopy` 契约、`src/lib/useEngineInvalidation.ts` 的 rejection 处理（floating promise 规则正是守它们的那一类）"
  prohibitions:
    - "第三方包在未经合法性核对前不得进入依赖树"
    - "密钥（LLM API key / MCP bearer token）不得进入 SQLite、日志、错误文本或前端 DOM"
---

<objective>
关闭上轮 WR-16 的后半段：**仓库里没有任何 ESLint 配置**。`npm run build` 确实跑 `tsc --noEmit`（类型检查有闸门，`noUnusedLocals` / `noUnusedParameters` 已开），但 tsc 结构性看不见的那一类问题一条也没人守：条件分支里的 hook 调用、缺失的 hook 依赖、悬空 promise、`no-console`——而这是一个刻意把每个错误都路由过 `errorCopy` 的代码库。

上轮 WR-16 点名的悬空 promise 尤其贴切：`src/lib/useEngineInvalidation.ts` 第 15-19 行的注释写着「这一类失败不能被丢进未处理的 Promise」，而现在没有任何自动化的东西在守这句话。

Purpose: 前端是本项目唯一没有 lint 闸门的语言面（Rust 侧有 `clippy -D warnings`，只是覆盖集不全——那一半由 01-27 关闭）。

Output: 一份最小但有牙的 ESLint flat config，与一个可被 CI 调用的 `npm run lint`。

**本 plan 引入新的 npm 依赖，因此含一个 `gate="blocking-human"` 的包合法性闸门。** 未经审计的包一律按 `[ASSUMED]` 处理，闸门不可自动放行（`workflow.auto_advance` 对它无效）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@package.json
@tsconfig.json
@.planning/phases/01-foundation-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-skeleton/01-REVIEW-prior.md
</context>

<why_no_tracer>
缺口修补集，无新功能垂直切片可领；本轮按主题横向聚类，`TRACER_MODE` 不适用（理由同 01-14）。
</why_no_tracer>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 包合法性闸门 —— 新增 npm 依赖的人工核对</name>
  <read_first>
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Package Legitimacy Audit（第 179-212 行）—— 表的列结构、既有条目的写法、以及 `tracing-subscriber` 补录说明的形态（01-13 执行期的同类闸门先例）
    - `.planning/STATE.md` 「01-13: 包合法性闸门不可自动放行（即使 auto_advance 为真）」一条 —— 本闸门沿用同一纪律
  </read_first>
  <what-built>
    尚未安装任何包。本闸门排在安装**之前**。

    本 plan 拟新增的 npm devDependencies（全部为 `[ASSUMED]` —— 不在 `01-RESEARCH.md` § Package Legitimacy Audit 表中）：

    | 包名 | 用途 | 拟 pin |
    |---|---|---|
    | `eslint` | lint 引擎本体 | 最新稳定 major |
    | `typescript-eslint` | TypeScript 解析器 + 规则集（flat config 的统一入口包） | 与 eslint major 兼容的最新稳定版 |
    | `eslint-plugin-react-hooks` | hook 依赖与调用规则（`rules-of-hooks` / `exhaustive-deps`） | 最新稳定版 |
    | `globals` | flat config 下声明浏览器/Node 全局（`typescript-eslint` 的常见配套） | 最新稳定版 |

    实际安装前，执行者需先在 registry 上确认每一个包的确切名字、当前稳定版本与仓库地址，并把待核对清单填成上表的完整形态（含首发年份、周下载量、源仓库、是否 yank、许可、是否含 `postinstall` 脚本）。
  </what-built>
  <how-to-verify>
    请逐包在 npmjs.com 上打开对应页面并确认下列各项，然后回复 `approved` 或指出问题：

    1. `https://www.npmjs.com/package/eslint` —— 名字逐字为 `eslint`（无形近名）；仓库指向 `eslint/eslint`；周下载量量级；许可；无可疑 `postinstall`
    2. `https://www.npmjs.com/package/typescript-eslint` —— 名字逐字为 `typescript-eslint`（注意它与旧的 `@typescript-eslint/eslint-plugin` + `@typescript-eslint/parser` 两包形态不同，flat config 时代的统一入口就叫这个）；仓库指向 `typescript-eslint/typescript-eslint`
    3. `https://www.npmjs.com/package/eslint-plugin-react-hooks` —— 仓库指向 `facebook/react`（它随 React 一同发布）
    4. `https://www.npmjs.com/package/globals` —— 仓库指向 `sindresorhus/globals`

    每一项都要看：**名字逐字对**（形近名是供应链攻击最常见的入口）、**仓库归属**、**周下载量**、**最近发版时间**、**许可**、**是否被 yank/deprecate**。

    执行者应把在 registry 上读到的实际数字先贴出来供你比对，不要只贴结论。
  </how-to-verify>
  <acceptance_criteria>
    - 用户明确回复 `approved`（或逐包指出问题）后才进入 Task 2
    - 核对结论按 `01-RESEARCH.md` § Package Legitimacy Audit 表的既有列结构写回该表（Package / Registry / Age / Downloads / Source Repo / Verdict / Disposition），并按 `tracing-subscriber` 那条的形态补一段说明，注明核对日期与人工确认的事实
    - 任何 `[SLOP]` 判定的包**禁止**引入；`[SUS]` 判定的包需在说明里写明反向信号
    - 本闸门不可自动放行——即使 `workflow.auto_advance` 为真也必须等待人工回复
  </acceptance_criteria>
  <resume-signal>逐包核对后回复 "approved"，或指出哪一个包有问题</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 落地 ESLint flat config 与 npm run lint</name>
  <files>eslint.config.js, package.json, package-lock.json</files>
  <read_first>
    - `package.json` 全文（现有 scripts 与 devDependencies；注意 `build` 是 `tsc --noEmit && vite build`）
    - `tsconfig.json`（`strict` / `noUnusedLocals` / `noUnusedParameters` 已开——ESLint 的对应规则要避免与它重复报同一件事）
    - `src/lib/useEngineInvalidation.ts` 第 15-19 行 —— floating promise 规则要守的那段注释
    - `src/lib/ipc.ts`、`src/pages/Settings.tsx`、`src/pages/DevSmoke.tsx`（本轮 01-22 / 01-23 刚改过）—— lint 首跑最可能出问题的地方
    - `src/pages/Settings.test.tsx` 第 41-43 行 —— vitest globals 未开启、手工注册 `afterEach(cleanup)`；lint 配置对测试文件的全局声明要与此一致
    - `.planning/phases/01-foundation-skeleton/01-REVIEW-prior.md` 上轮 WR-16 —— 点名了要覆盖的四类规则
  </read_first>
  <action>
    安装 Task 1 已批准的包为 devDependencies，pin 到批准时确认的版本线。

    新建 `eslint.config.js`（flat config，ESM——`package.json` 已是 `"type": "module"`），覆盖 `src/**/*.{ts,tsx}`。规则集取「最小但有牙」：

    - `typescript-eslint` 的推荐集（含类型感知规则，需要在配置里指向 `tsconfig.json`；若类型感知规则让首跑过慢或报错过多，可退到非类型感知的推荐集，但**必须**保留 `no-floating-promises` 这一条——它是上轮 WR-16 点名的那条，也是 `useEngineInvalidation.ts` 注释里那句话的自动化形态）
    - `eslint-plugin-react-hooks` 的 `rules-of-hooks` 与 `exhaustive-deps`，**两条都必须是 error**——反证 ①② 的判据是 `npm run lint` 退出 1，而 eslint 对 warn 级别的命中仍退出 0（除非加 `--max-warnings 0`）；配成 warn 会让这两条反证在规则确实生效时也退出 0，判别力归零
    - `no-console`（error，允许 `console.error` 与否二选一并写明理由——这个代码库刻意把每个错误路由过 `errorCopy`）
    - 关闭与 `tsconfig.json` 重复的规则（`noUnusedLocals` / `noUnusedParameters` 已由 tsc 覆盖；重复报同一件事只会制造噪声）

    **规则集与反证的取向说明**（写进 `eslint.config.js` 的文件头注释）：上一条关掉了 unused-vars 一类规则，因此本 plan 的三条反证**一律不用未使用变量做样本**——那条已由 `tsc --noEmit` 守，而 `lint` script 是 `eslint .`、不含 tsc，用它做反证必然退出 0。三条反证的目标规则全部取 **ESLint 独有、tsc 结构性看不见**的那一类：`react-hooks/rules-of-hooks`（条件分支里调 hook 是运行时 bug，类型系统看不见）、`react-hooks/exhaustive-deps`、`@typescript-eslint/no-floating-promises`。这样「关闭重复规则」与「反证 ESLint 真的在跑」两个目标不再打架，且每条反证同时证明 ESLint 带来了 tsc 之外的增量价值。
    （被否掉的另一条路是把 `no-unused-vars` 保留为 error 以便沿用原样本——它会让同一件事被 tsc 与 ESLint 各报一次，正是上一条自己要避免的噪声。）

    忽略集：`dist/`、`node_modules/`、`src-tauri/`（Rust 侧）、`coverage/`。

    在 `package.json` 加 `"lint": "eslint ."`（或带显式路径），**不要**把它塞进 `build`——`build` 的职责是构建，闸门接线由 01-27 在 CI 里做。

    首跑 `npm run lint` 并逐条处理报出的问题。**处理次序有约束**：优先修代码；只有当某条规则与本项目已确立的设计决策真正冲突时才调整规则配置，并在 `eslint.config.js` 里就地注释写明冲突的是哪条决策。**禁止**为了让 lint 过而削弱任何安全断言或改动本轮其他 plan 刚落地的实现语义（例如 01-23 的 `errorCopy` 自有属性判定、01-24 的精确相等断言）。

    在 `eslint.config.js` 顶部写一段文件头注释，说明这份配置存在的理由（前端是本项目唯一无 lint 闸门的语言面）与它守的四类问题，形态参照 `scripts/check-deps.sh` / `scripts/check-secrets.sh` 的文件头。
  </action>
  <verify>
    <automated>npm run lint && npm run build && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` 在当前代码库上退出 0
    - `npm run build`（含 `tsc --noEmit`）退出 0；`npm run test -- --run` 全绿
    - **非恒真反证（三条，全部必须实跑）**：分别往任一 `src/` 文件临时插入 —— ①把某个组件里的一次 `useState` 调用挪进一个 `if` 分支（目标规则 `react-hooks/rules-of-hooks`）、②一个缺依赖的 `useEffect`（目标规则 `react-hooks/exhaustive-deps`）、③一个未 `await` 也未 `void` 的 promise 调用（目标规则 `@typescript-eslint/no-floating-promises`）；每次跑 `npm run lint`，退出码必须是 1 且输出点名上述对应规则名。逐条 `git checkout` 还原。三次输出抄进 SUMMARY。**三条样本都不得用未使用变量**——该规则已按 `<action>` 关掉（由 tsc 覆盖），拿它做反证必然退出 0。若某条规则因退到非类型感知集而不可用（只可能是③），必须在 SUMMARY 里写明并给出替代的守法手段与一条替代反证（仍须取 ESLint 独有的规则）
    - 安装的包与版本与 Task 1 批准的一致：`npm ls eslint typescript-eslint eslint-plugin-react-hooks globals` 的输出抄进 SUMMARY
    - 本轮其他 plan 的实现语义未被 lint 驱动改动：`git diff` 里 `src/lib/ipc.ts` / `src/lib/tauri-security.test.ts` / `src/lib/capabilities.test.ts` / `src/pages/Settings.tsx` 若有改动，逐条在 SUMMARY 里说明是哪条规则要求的以及为什么不构成语义削弱
    - `bash scripts/check-secrets.sh all` 退出 0（新增的 lock 文件条目不含命中扫描器的字面量）
    - 收尾 `git status --porcelain` 里只剩本 plan 的三个文件（加 `01-RESEARCH.md` 的审计表更新）
  </acceptance_criteria>
  <done>前端有了一条能抓条件分支里的 hook 调用、缺失 hook 依赖与悬空 promise 的闸门——三条 tsc 结构性看不见的问题——且它的判别力由三条实跑反证证明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → 本项目依赖树 | 形近名与恶意 `postinstall` 是供应链攻击的主要入口；未经审计的包一律按 `[ASSUMED]` 处理 |
| lint 规则 → 代码语义 | 一条为了让 lint 过而做的「修复」可能削弱一条安全断言 |
| 悬空 promise → 静默失败 | `useEngineInvalidation` 的 rejection 若被丢进未处理 Promise，表现为「数据一直是旧的」，与「数据本来就没变」同形 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs（`eslint` / `typescript-eslint` / `eslint-plugin-react-hooks` / `globals`） | high | mitigate | Task 1 的 `gate="blocking-human"` 闸门：安装前逐包核对名字/仓库/下载量/许可/postinstall，结论写回审计表；闸门不可自动放行 |
| T-01G-55 | Repudiation | 前端无 lint 闸门 | medium | mitigate | Task 2：flat config 覆盖四类问题；配三条实跑反证证明判别力 |
| T-01G-56 | Denial of Service | 悬空 promise 让失效链路静默失败 | medium | mitigate | Task 2：`no-floating-promises` 保留为硬约束；退档时必须给出替代守法手段 |
| T-01G-57 | Elevation of Privilege | 为过 lint 而削弱安全断言 | high | mitigate | Task 2 明确禁止；配「其他 plan 文件若有改动须逐条说明」的验收判据 |
</threat_model>

<verification>
```bash
npm run lint
npm run build
npm run test -- --run
npm ls eslint typescript-eslint eslint-plugin-react-hooks globals
bash scripts/check-secrets.sh all
```

三条非恒真反证（`if` 分支里的 `useState` / 缺依赖 `useEffect` / 悬空 promise）必须逐条实跑并把输出抄进 SUMMARY。三条都取 ESLint 独有、tsc 看不见的规则——未使用变量已由 `tsc --noEmit` 覆盖且对应 ESLint 规则已关，不可用作反证样本。
</verification>

<success_criteria>
- 新增 npm 包全部经人工合法性核对，结论写回审计表
- `npm run lint` 存在、当前退出 0、且对三类目标问题各有一条实跑反证
- 本轮其他 plan 的实现语义未被 lint 驱动削弱
</success_criteria>

<artifacts_this_phase_produces>
| 类别 | 名称 | 位置 |
|---|---|---|
| 文件（新） | `eslint.config.js` —— flat config（ESM），覆盖 `src/**/*.{ts,tsx}` | 仓库根 |
| npm script（新） | `lint` | `package.json` |
| devDependencies（新） | `eslint` / `typescript-eslint` / `eslint-plugin-react-hooks` / `globals` | `package.json` + `package-lock.json` |
| 审计表行（新） | 上述四个包的合法性核对结论 + 一段人工确认说明 | `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Package Legitimacy Audit |

CI 接线（frontend job 的 lint 步骤）**不在**本 plan——由 01-27 完成，避免与它抢 `.github/workflows/ci.yml`。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-foundation-skeleton/01-26-SUMMARY.md` when done.

SUMMARY 必须包含：Task 1 闸门上人工核对的**实际读数**（每包的下载量/发版时间/仓库/许可）、最终 pin 的版本、三条反证的输出，以及规则集是否退档（若退档，替代守法手段是什么）。
</output>
