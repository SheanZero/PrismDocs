---
phase: 01-foundation-skeleton
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-04"]
files_modified:
  - crates/prism-mcp/Cargo.toml
  - crates/prism-mcp/src/lib.rs
  - crates/prism-mcp/src/deps.rs
  - crates/prism-mcp/src/middleware.rs
  - crates/prism-mcp/src/handler.rs
  - crates/prism-mcp/src/server.rs
  - crates/prism-mcp/tests/trait_injection.rs
  - crates/prism-mcp/tests/middleware_gate.rs
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "prism-mcp 只经注入的 service trait 取数据，编译期不可能依赖 prism-engine：`cargo tree -p prism-mcp --edges normal` 中无 prism-engine，且 `cargo test -p prism-mcp trait_injection` 用一个假 FeedbackSource 实现证明注入通路可用"
    - "MCP server 绑定在 127.0.0.1 上由 OS 分配端口，不监听任何对外地址"
    - "Host header 不在 {127.0.0.1, localhost} 内的请求得到 403，即使携带正确 bearer（DNS-rebinding 防护）"
    - "Origin header 不在 allowlist 内的请求得到 403，即使 Host 与 bearer 均正确"
    - "bearer 缺失或错误的请求得到 401/403，即使 Host 与 Origin 均正确"
    - "三层全部满足的请求可到达 mcp service（非 4xx）——证明三条中间件不是把所有请求都拒了"
    - "bearer 比较为常数时间：中间件使用 subtle 的常数时间比较而非 `==`"
  artifacts:
    - path: "crates/prism-mcp/src/deps.rs"
      provides: "McpDeps 注入容器（Arc<dyn FeedbackSource> + Arc<dyn CommentSink> + bearer token）"
      exports: ["McpDeps"]
    - path: "crates/prism-mcp/src/middleware.rs"
      provides: "require_local_host / require_origin_allowlist / require_bearer 三层中间件"
      exports: ["require_local_host", "require_origin_allowlist", "require_bearer"]
      min_lines: 50
    - path: "crates/prism-mcp/src/server.rs"
      provides: "build_router（StreamableHttpService 挂载 + 三层中间件）与 serve_loopback"
      exports: ["build_router", "serve_loopback"]
    - path: "crates/prism-mcp/tests/trait_injection.rs"
      provides: "假 FeedbackSource 注入路径测试"
      contains: "FeedbackSource"
    - path: "crates/prism-mcp/tests/middleware_gate.rs"
      provides: "Host / Origin / bearer 三层 403 与放行的四组断言"
      min_lines: 60
  key_links:
    - from: "crates/prism-mcp/src/server.rs"
      to: "rmcp::transport::streamable_http_server::StreamableHttpService"
      via: "Router::nest_service(\"/mcp\", svc) 挂载官方 service"
      pattern: "nest_service"
    - from: "crates/prism-mcp/src/handler.rs"
      to: "prism_types::FeedbackSource"
      via: "handler 在 spawn_blocking 中调用注入的同步 trait"
      pattern: "spawn_blocking"
    - from: "crates/prism-mcp/src/server.rs"
      to: "crates/prism-mcp/src/middleware.rs"
      via: "axum::middleware::from_fn 叠加三层校验"
      pattern: "from_fn"
  prohibitions: []
---

<objective>
建立 prism-mcp 的 trait 反转骨架（D-09）与 D-07 的三层鉴权形态：rmcp `StreamableHttpService`
挂在 axum 0.8 的 loopback router 上，数据只经注入的 `Arc<dyn FeedbackSource>` 取得。

Purpose: D-09 的价值是**编译期**保证——`prism-engine` 依赖 `prism-mcp`，因此一旦 `prism-mcp`
声明对 `prism-engine` 的普通依赖就是 cargo 硬错误 `cyclic package dependency`，环不可能存在；
唯一的逃逸口是 dev-dependency 环，已由 plan 01-02 的 `--edges normal` 断言堵死。
D-07 的三层防护（Host + Origin + bearer）形态现在定死，Phase 6 只往里加工具，不动这层。

Output: `McpDeps` 注入容器、三层中间件、`build_router` / `serve_loopback`、两组集成测试。

Phase 1 交付边界：工具面、端口发现、CLI helper 契约全部是 Phase 6 的内容——本 plan 只需要一个
能编译、能起 axum、能通过一次注入 trait 返回假数据的最小 handler。能力面的逐项取舍记录在
`COVERAGE.md`。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-skeleton/01-VALIDATION.md
@.planning/phases/01-foundation-skeleton/COVERAGE.md
@.planning/phases/01-foundation-skeleton/01-04-SUMMARY.md
</context>

## Artifacts this phase produces

本 plan 新建的符号：

- **模块**：`prism_mcp::deps`、`prism_mcp::middleware`、`prism_mcp::handler`、`prism_mcp::server`
- **类型与函数**：`McpDeps`、`PrismHandler`、`McpError`、`build_router()`、`serve_loopback()`、
  `require_local_host()`、`require_origin_allowlist()`、`require_bearer()`
- **常量**：`MCP_MOUNT_PATH`（值 `/mcp`）、`ALLOWED_HOSTS`、`ALLOWED_ORIGINS`、`LOOPBACK_BIND`（值 `127.0.0.1:0`）
- **测试名**：`injected_feedback_source_is_reached`、`handler_never_touches_engine_types`、
  `rejects_foreign_host`、`rejects_foreign_origin`、`rejects_missing_or_wrong_bearer`、
  `accepts_fully_valid_request`

从兄弟 plan 消费的符号：`prism_types::{FeedbackSource, CommentSink, FeedbackItem, Receipt, ServiceError}`（plan 01-04）。

<tasks>

<task type="auto">
  <name>Task 1: McpDeps 注入容器、最小 handler 与 rmcp/axum 挂载形态</name>
  <reversibility rating="costly">`Arc<dyn Trait>` 注入（而非泛型参数 `S: FeedbackSource`）决定 prism-mcp 的公开类型不被 engine 具体类型污染，也决定 `StreamableHttpService::new` 的 factory 闭包写法；改成泛型要重写 build_router 签名与 Phase 6 的全部工具注册点。无数据迁移、无已发布契约，故为 costly。</reversibility>
  <files>crates/prism-mcp/Cargo.toml, crates/prism-mcp/src/lib.rs, crates/prism-mcp/src/deps.rs, crates/prism-mcp/src/handler.rs, crates/prism-mcp/src/server.rs, crates/prism-mcp/tests/trait_injection.rs</files>
  <read_first>
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Pattern 4（prism-types 同步 trait 反转）——依赖方向图、`Arc<dyn>` vs 泛型的取舍、`spawn_blocking` 调用形态、rmcp 2.2 + axum 0.8 的官方挂载代码、Phase 1 交付边界
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Standard Stack → Core（rmcp 的确切 feature 名：`server` 与 `transport-streamable-http-server`，本 session 已从 crates.io manifest 直读核验，解除 STATE.md 的 Phase 1 待办）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Don't Hand-Roll（SSE / MCP 传输交给 `StreamableHttpService` + `LocalSessionManager`，不手写会话管理）
    - `.planning/phases/01-foundation-skeleton/01-CONTEXT.md` § Crate 骨架完整度 → D-09
    - `.planning/phases/01-foundation-skeleton/COVERAGE.md`（本阶段 MCP 能力面的逐项决定——工具面全部 OPT-OUT 到 Phase 6）
    - `crates/prism-types/src/service.rs`（plan 01-04 定义的 trait 签名）
    - `crates/prism-mcp/src/lib.rs`（plan 01-01 建立的 `protocol_version()` 最小编译单元现状）
  </read_first>
  <action>
    `Cargo.toml`：`rmcp`（**features 必须是 `["server", "transport-streamable-http-server"]`**，
    此名称已从 crates.io 的 rmcp 2.2.0 manifest 直读核验，STATE.md 里「rmcp feature-flag 待核验」
    的待办据此关闭）、`axum`、`tokio`、`tokio-util`（CancellationToken）、`prism-types`、`subtle`、
    `serde`、`serde_json`、`thiserror`、`tracing`；`[dev-dependencies]` 加 `tokio`（`macros` + `rt-multi-thread`）
    与 `reqwest`（仅测试用，发本地请求）。
    **绝不声明对 prism-engine 的依赖**——普通依赖会构成 cargo 硬错误，dev 依赖会被 plan 01-02 的
    `--edges normal` 断言抓住。

    `src/deps.rs`：`#[derive(Clone)] pub struct McpDeps { pub feedback: Arc<dyn FeedbackSource>,
    pub comments: Arc<dyn CommentSink>, pub bearer: Arc<str> }`。用 `Arc<dyn …>` 而不是泛型参数：
    泛型会把 engine 的具体类型经 `S` 泄漏进 axum service 的类型签名，且
    `StreamableHttpService::new` 的 `'static` factory 闭包更难写；`Arc` 方案下 factory 只是 clone 一个 Arc。
    bearer token 由调用方（Phase 1 是测试，Phase 6 是 prism-engine 从钥匙串读）注入——
    **prism-mcp 自己不生成也不存储 token**。

    `src/handler.rs`：`PrismHandler`（持 `McpDeps`）实现 rmcp 的 server handler。Phase 1 只需要
    一个最小方法：从注入的 `feedback` 取数据并返回。因为 service trait 是**同步**的而 rmcp handler
    是 async 的，调用形态必须是先 clone 出 `Arc`，再
    `tokio::task::spawn_blocking(move || svc.list_feedback(&pid)).await` 并处理 join error——
    **不得在 async 上下文里直接阻塞调用**。handler 内不得出现任何 prism-engine 类型。

    `src/server.rs`：
    - `pub fn build_router(deps: McpDeps, ct: tokio_util::sync::CancellationToken) -> axum::Router`：
      用 `StreamableHttpService::new(move || Ok(PrismHandler::new(deps.clone())),
      LocalSessionManager::default().into(), StreamableHttpServerConfig::default().with_cancellation_token(ct.child_token()))`
      构造 service，`Router::new().nest_service(MCP_MOUNT_PATH, svc)`，再叠三层
      `axum::middleware::from_fn`（Task 2 提供）。形态逐行照 rmcp 官方示例
      `examples/servers/src/counter_streamhttp.rs`。
    - `pub async fn serve_loopback(deps, ct) -> Result<(SocketAddr, JoinHandle<()>), McpError>`：
      `TcpListener::bind(LOOPBACK_BIND)`（`127.0.0.1:0`，由 OS 分配端口——端口策略是 Phase 6 的事），
      读回实际地址后 `axum::serve` 起在后台任务里。**只绑 127.0.0.1，不绑 0.0.0.0**。

    `tests/trait_injection.rs`：定义一个假 `FeedbackSource` 实现（返回固定的一条 `FeedbackItem`）
    与假 `CommentSink`，装进 `McpDeps`，起 `serve_loopback`，发一次带完整合法头的请求，
    断言响应中出现假实现返回的那条数据——这证明「prism-mcp 经注入 trait 取数据」这条通路真的通，
    而不只是能编译。
  </action>
  <verify>
    <automated>cargo test -p prism-mcp --test trait_injection &amp;&amp; ! cargo tree -p prism-mcp --edges normal --prefix none | tail -n +2 | grep -q '^prism-engine '</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p prism-mcp --test trait_injection` 退出 0，`injected_feedback_source_is_reached` 通过
    - `cargo tree -p prism-mcp --edges normal --prefix none | tail -n +2 | grep -c '^prism-engine '` 输出 0
    - `cargo tree -p prism-mcp --edges dev --prefix none | tail -n +2 | grep -c '^prism-engine '` 输出 0（dev 逃逸口也未被使用）
    - `crates/prism-mcp/Cargo.toml` 的 rmcp 一行同时包含 `"server"` 与 `"transport-streamable-http-server"`
    - `crates/prism-mcp/src/handler.rs` 中 `spawn_blocking` 出现 ≥1 次
    - `crates/prism-mcp/src/server.rs` 中 `nest_service` 出现 1 次，`127.0.0.1` 出现 ≥1 次，`0.0.0.0` 出现 0 次
    - `crates/prism-mcp/src/deps.rs` 中 `Arc<dyn FeedbackSource>` 与 `Arc<dyn CommentSink>` 各出现 ≥1 次（无泛型参数形式）
    - `serve_loopback` 返回的 `SocketAddr` 的 IP 等于 `127.0.0.1` 且端口非 0（单测断言）
    - `cargo clippy -p prism-mcp -- -D warnings` 退出 0
    - `bash scripts/check-deps.sh no-cycle` 与 `bash scripts/check-deps.sh tauri-free` 退出 0
  </acceptance_criteria>
  <done>prism-mcp 可编译、可起 axum loopback server、可经注入的 `Arc<dyn FeedbackSource>` 返回假数据；依赖树中无 prism-engine（普通边与 dev 边都没有）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Host / Origin / bearer 三层中间件与 403 门禁测试</name>
  <files>crates/prism-mcp/src/middleware.rs, crates/prism-mcp/src/server.rs, crates/prism-mcp/tests/middleware_gate.rs</files>
  <read_first>
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Pattern 4 → 「中间件顺序」段（axum `.layer()` 由内向外叠加；Host + Origin allowlist + bearer 三层缺一即 403）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Security Domain → V2 / V4（bearer ≥256-bit、**常数时间比较不用 `==`**；Host∈{127.0.0.1, localhost} + Origin allowlist + bearer 三层；loopback-only 绑定）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § State of the Art → Deprecated（rmcp <1.4.0 的 DNS-rebinding CVE GHSA-89vp-x53w-74fx；SDK 修复**不替代**应用层 allowlist）
    - `.planning/phases/01-foundation-skeleton/01-RESEARCH.md` § Assumptions Log → A5（axum 0.8 `.layer()` 叠加方向为推断，需以实测为准）
    - `crates/prism-mcp/src/server.rs`（Task 1 建立的 `build_router` 现状）
  </read_first>
  <behavior>
    - `rejects_foreign_host`：Host 头为 `evil.example.com`、Origin 与 bearer 均合法 → 响应 403
    - `rejects_foreign_origin`：Origin 头为 `https://evil.example.com`、Host 与 bearer 均合法 → 响应 403
    - `rejects_missing_or_wrong_bearer`：无 Authorization 头 → 4xx；Authorization 为错误 token → 4xx；
      两种情况下 Host 与 Origin 均合法
    - `accepts_fully_valid_request`：Host、Origin、bearer 三者全对 → 响应状态码非 4xx
      （证明三层不是把所有请求都拒了——这是这组测试的阴性对照）
    - 长度不同的错误 bearer 与长度相同但内容不同的错误 bearer 都被拒（比较不因长度短路而泄漏信息）
  </behavior>
  <action>
    新建 `crates/prism-mcp/src/middleware.rs`，实现 D-07 要求的三层校验，每层都是
    `axum::middleware::from_fn` 可用的 async 函数，任一不满足即返回 403（bearer 缺失时用 401 更准确，
    但两者都必须是 4xx 且不泄漏「哪一层挂了」的细节）。

    - `require_local_host`：读 `Host` 头，取出主机名部分（剥掉端口），必须在常量
      `ALLOWED_HOSTS = ["127.0.0.1", "localhost", "[::1]"]` 内。这是 DNS-rebinding 防护——
      rmcp <1.4.0 有过真实 CVE，本项目 pin 2.2 已含上游修复，但**SDK 修复不替代应用层 allowlist**。
    - `require_origin_allowlist`：若存在 `Origin` 头，其值必须匹配常量 `ALLOWED_ORIGINS`
      （`http://127.0.0.1` / `http://localhost` 及其带端口形式，以及 Tauri WebView 的
      `tauri://localhost`）。无 Origin 头的请求（非浏览器客户端，如 Claude Code）放行到下一层。
    - `require_bearer`：读 `Authorization` 头，必须是 `Bearer ` 前缀 + 与 `McpDeps.bearer` 相等的
      token。比较**必须用 `subtle` 的常数时间比较**（`ConstantTimeEq` / `ct_eq`），
      **不得用 `==`**（Security Domain V2）。长度不等时也要走一条不因长度提前返回的路径。
      Phase 1 的 token 由注入方提供；生成（CSPRNG ≥256-bit）与钥匙串存取是 Phase 6。

    在 `build_router` 中用 `.layer(from_fn(...))` 叠加三层。RESEARCH 的 A5 假设「axum 0.8 的
    `.layer()` 后加先执行」未经直查，因此**不要依赖顺序来保证安全性**——三层都必须独立成立，
    并由 `tests/middleware_gate.rs` 的四组断言实测。若实测发现顺序与预期不同，在 SUMMARY 里记录
    实际观察到的顺序以关闭 A5。

    `tests/middleware_gate.rs` 按 `<behavior>` 写全四个测试：起 `serve_loopback`，用 `reqwest`
    对分配到的端口发请求，逐项构造「只有一层不合法」的请求并断言 4xx，最后一个测试构造全合法请求
    并断言非 4xx。
  </action>
  <verify>
    <automated>cargo test -p prism-mcp --test middleware_gate &amp;&amp; cargo test -p prism-mcp</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p prism-mcp --test middleware_gate` 退出 0，四个测试全部执行
    - `cargo test -p prism-mcp --test middleware_gate rejects_foreign_host` 单独运行退出 0
    - `cargo test -p prism-mcp --test middleware_gate rejects_foreign_origin` 单独运行退出 0
    - `cargo test -p prism-mcp --test middleware_gate rejects_missing_or_wrong_bearer` 单独运行退出 0
    - `cargo test -p prism-mcp --test middleware_gate accepts_fully_valid_request` 单独运行退出 0（阴性对照：不是全拒）
    - `crates/prism-mcp/src/middleware.rs` 中 `ct_eq` 或 `ConstantTimeEq` 出现 ≥1 次，
      且 bearer 值的比较处不使用 `==`
    - 反证：临时把 `require_local_host` 从 `build_router` 中摘掉后 `rejects_foreign_host` 变红；恢复后转绿
    - 反证：临时把 `require_bearer` 摘掉后 `rejects_missing_or_wrong_bearer` 变红；恢复后转绿
    - SUMMARY 中记录实测到的中间件执行顺序（关闭 RESEARCH Assumptions Log 的 A5）
    - `cargo clippy -p prism-mcp -- -D warnings` 退出 0
  </acceptance_criteria>
  <done>Host / Origin / bearer 三层中间件各自独立生效（各有一条只违反该层的 403 测试），全合法请求可通过，bearer 比较为常数时间；A5 假设经实测关闭。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部 agent / 浏览器页面 → loopback MCP server（127.0.0.1） | 本机上任何进程与任何被访问的网页都能向这个端口发请求；此边界是 Phase 6 全部攻击面的地基 |
| prism-mcp → 注入的 service trait | prism-mcp 不持有数据，只持有 `Arc<dyn …>`；数据边界由 trait 签名定义 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Spoofing | DNS rebinding 到 127.0.0.1 的 MCP server（rmcp <1.4.0 曾有真实 CVE GHSA-89vp-x53w-74fx） | high | mitigate | `require_local_host` 校验 Host∈{127.0.0.1, localhost, [::1]} + `require_origin_allowlist`；pin 的 rmcp 2.2 已含上游修复但不替代应用层 allowlist；`rejects_foreign_host` / `rejects_foreign_origin` 两个测试锁死并有摘除即红的反证 |
| T-01-06 | Spoofing | bearer token 被时序侧信道逐字节猜出 | high | mitigate | 用 `subtle` 的常数时间比较，禁止 `==`；长度不等时也走不提前返回的路径；验收项断言源码中出现 `ct_eq`/`ConstantTimeEq` |
| T-01-28 | Information Disclosure | MCP server 绑定到非 loopback 地址，局域网可达 | high | mitigate | `LOOPBACK_BIND` 常量固定为 `127.0.0.1:0`；验收项断言源码中 `0.0.0.0` 出现 0 次、`serve_loopback` 返回的 IP 为 127.0.0.1 |
| T-01-29 | Information Disclosure | 4xx 响应体泄漏「是哪一层拒绝的」，帮助攻击者逐层试探 | medium | mitigate | 三层统一返回不含细节的 4xx；具体拒绝原因只进 `tracing` 本地日志 |
| T-01-17 | Elevation of Privilege | prism-mcp 经 dev 依赖反向依赖 prism-engine，绕开 D-09 | medium | mitigate | 普通依赖环是 cargo 硬错误；dev 边由 plan 01-02 的 `--edges normal` 断言 + 本 plan 的 `--edges dev` 验收项双重堵死 |
| T-01-30 | Denial of Service | rmcp session 未随应用关闭而清理，端口/内存泄漏 | low | accept | `StreamableHttpServerConfig::with_cancellation_token` 接入 graceful shutdown；单用户本地场景下残余风险可接受，Phase 6 落地工具面时复核 |
</threat_model>

<verification>
- `cargo test -p prism-mcp` 全绿（trait_injection + middleware_gate 五项）
- `cargo clippy -p prism-mcp -- -D warnings` 退出 0
- `bash scripts/check-deps.sh no-cycle` 与 `bash scripts/check-deps.sh tauri-free` 退出 0
- 两条反证（摘掉 Host 层、摘掉 bearer 层）各自使对应测试变红
- SUMMARY 记录实测的中间件执行顺序（关闭 A5）与 rmcp feature 名核验结论（关闭 STATE.md 的 Phase 1 待办）
</verification>

<success_criteria>
成功标准 1 的第三条：prism-mcp 仅依赖注入的 service trait，编译期无 facade↔mcp 依赖环，
且注入通路有实测证据；D-07 的三层鉴权形态定死并各有独立的 403 证据。
</success_criteria>

<output>
Create `.planning/phases/01-foundation-skeleton/01-06-SUMMARY.md` when done
</output>
