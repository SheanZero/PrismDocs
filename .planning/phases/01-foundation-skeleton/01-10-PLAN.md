---
phase: 01-foundation-skeleton
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/prism-store/src/settings.rs
  - src/lib/ipc.ts
  - src/pages/Settings.tsx
  - src/pages/Settings.test.tsx
autonomous: true
gap_closure: true
requirements: [INFRA-02, INFRA-03]

must_haves:
  truths:
    - "带 userinfo 的 base_url 在 engine 写入路径上被拒：`cargo test -p prism-store settings_base_url_rejects_credential_bearing_values` 退出 0——凭据型 URL 返回 `Err(StoreError::InvalidUrl)`，且随后 `SELECT count(*) FROM settings` 为 0"
    - "只带用户名、不带密码的 URL 同样被拒（`url.username()` 非空即拒，不依赖 password 存在）"
    - "带非空 query 或非空 fragment 的 base_url 被拒——部分网关用 `?api-key=…` 形态传凭据，那是同一个口子的另一面"
    - "拒绝理由不回显被拒的值：错误 `Display` 中既不含用户名子串也不含密码子串（T-01-26 同源要求）"
    - "阴性对照成立：`https://api.example.com/v1` 仍被接受并写入，表内恰好 1 行——守卫不是「一律拒绝」"
    - "同一个带凭据的 base_url 连写两次，两次都返回 Err 且 settings 表行数始终为 0——守卫无状态，首次拒绝不会让第二次通过"
    - statement: "并发写入下凭据守卫仍在单写者事务内先于 tx.execute 生效——被拒的写入不留下部分状态，settings 表行数不变（本 plan 只在顺序路径上断言这一点，未新增并行用例）"
      verification: backstop
    - "前端在提交前本地拒绝带凭据的端点：`npm run test -- --run src/pages/Settings.test.tsx` 退出 0，其中带凭据的 URL 使 `setBaseUrl` 零次被调用、`role=\"alert\"` 呈现中文规则文案，且输入过的密码子串不出现在 DOM"
    - "前端阴性对照：干净的 `https://api.example.com/v1` 仍会调用 `setBaseUrl` 并显示成功文案"
    - "两道守卫各自可被掏空反证：删掉 `validate_base_url` 的 userinfo 分支后 `cargo test -p prism-store settings` 变红；删掉 `Settings.tsx` 的本地凭据检查后前端新测试变红"
    - "`docs/keychain-naming.md` 不变量 1（密钥绝不进 settings 表）在值侧成立——不再存在「键名不像密钥、值里带凭据」的通行路径"
  artifacts:
    - path: "crates/prism-store/src/settings.rs"
      provides: "settings k/v 读写 + base_url 校验，本 plan 把校验面从 scheme/host 扩到 userinfo/query/fragment（值侧守卫）"
      exports: ["validate_base_url", "set_setting", "get_setting", "is_secret_like_key"]
      min_lines: 250
    - path: "src/pages/Settings.tsx"
      provides: "设置页；base_url 提交前的本地体验层校验，返回错误码而非布尔"
      exports: ["default (SettingsPage)"]
      min_lines: 195
    - path: "src/lib/ipc.ts"
      provides: "命令封装 + 错误码→中文文案表，本 plan 新增凭据型 URL 的专用文案键"
      exports: ["errorCopy", "setBaseUrl", "SETTING_BASE_URL"]
      min_lines: 127
    - path: "src/pages/Settings.test.tsx"
      provides: "设置页交互测试；本 plan 新增凭据型端点的前端阴性/阳性对照"
      min_lines: 155
  key_links:
    - from: "crates/prism-store/src/settings.rs"
      to: "url::Url"
      via: "解析后逐项检查 userinfo / query / fragment，而不再只看 scheme 与 host"
      pattern: "url.password()"
    - from: "src/pages/Settings.tsx"
      to: "src/lib/ipc.ts"
      via: "本地校验产出稳定错误码，文案仍只在 ERROR_COPY 里生成"
      pattern: "invalid_url_credentials"
    - from: "src/pages/Settings.tsx"
      to: "crates/prism-store/src/settings.rs"
      via: "set_base_url 命令 → Engine → set_setting 写入路径上的权威校验（前端只是体验层）"
      pattern: "setBaseUrl"
  prohibitions:
    - requirement_id: INFRA-03
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT 让任何携带凭据的**值**进入 settings 表。密钥容器的边界必须建在值上而不只是键名上——sidecar 库按单目录整体备份（INFRA-05），一次备份就把凭据送出本机，这正是 docs/keychain-naming.md 不变量 1 给出的理由。"
    - requirement_id: INFRA-03
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT 在新增的凭据拒绝路径上回显被拒的值或其任何片段——错误消息、日志行、前端文案三处都只描述规则。被误填进 base_url 的那串东西很可能就是密钥本身。"
---

<objective>
关闭 01-VERIFICATION.md gap 1（Blocker）：`validate_base_url` 只看 scheme 与 host，
`https://user:密钥@host/v1` 这类形态原样落进 `settings.value`，把
`docs/keychain-naming.md` 不变量 1 与 plan 01-04 标为 `resolved` 的 privacy prohibition
一起推翻。verifier 已用探针实测复现，不是推演。

Purpose: 成功标准 4 第三分句「代码与配置中无明文密钥」在这里失守，INFRA-03 因此 BLOCKED。
守卫的边界建错了地方——`is_secret_like_key` 防的是**键名**，而 `llm.base_url` 这个键名
完全正常，凭据藏在**值**里。前端 `looksLikeHttpUrl` 是同一个形状的洞，两道守卫在同一处一起漏。

Output: `validate_base_url` 的值侧守卫 + 一条能被掏空即变红的 Rust 测试；
`Settings.tsx` 的本地校验从布尔改为错误码 + 一条前端阴性/阳性对照。

## 本轮 gap-closure 触及的 CONTEXT 决策（全 plan 集共用的账）

这是对一个**已执行完毕**的 phase 的缺口修补，CONTEXT.md 的十条决策早已由 plan 01-01…01-09
兑现；本轮只在其中四条上继续用力，其余六条不重新打开：

- **D-05**（非密钥配置存 SQLite settings 表，密钥仍走钥匙串、绝不入库）—— 本 plan 与 plan 01-11
  的全部工作都是这条决策后半句的执行机制：01-10 把边界从键名扩到值，01-11 让静态扫描真的看得见
  它要挡的东西。gap 1 与 gap 2 本质上是同一条决策的两处失守。
- **D-06**（前端交付 settings 页 + 隐藏 dev 冒烟页，配置可跳过）—— 本 plan Task 2 改的正是
  settings 页的端点校验；plan 01-13 的人工验证也在这两个页面上进行。改动不得让本页变得
  「必须先填对才能用」——可跳过这条性质一寸不动。
- **D-09**（service trait 定义在 prism-types，prism-mcp 不依赖 facade）—— plan 01-12 修改
  `McpDeps` 的构造契约时必须保住这条：注入形态仍是 `Arc<dyn …>`，依赖方向一条边不变。
- **D-01（架构口径 · engine 不依赖 tauri 可独立测试）**—— plan 01-13 新增的 `tracing-subscriber`
  必须只属于壳，engine crate 只发不装。注意与 CONTEXT.md 的 **D-01（FTS5 trigram 统一单索引）**
  同号不同事，本仓库两种编号并存已久（plan 01-05 已用「D-01（CONTEXT · trigram 单索引）」的写法
  消歧），此处沿用同一手法。

不重新打开的六条：**D-02**（短查询 LIKE 回退）、**D-03**（最小骨架 + 逐 phase 增量迁移）、
**D-04**（schema v1 最小集边界）、**D-07**（Phase 1 即引入 TanStack Query）、
**D-08**（全 crate 空骨架一次定型）、**D-10**（CLI helper 空占位 binary）——
它们各自的落地已由 01-VERIFICATION.md 的 SC-1/SC-2/SC-3 证据确认成立，本轮不产生任何相关改动。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-skeleton/01-VERIFICATION.md
@.planning/phases/01-foundation-skeleton/01-PATTERNS.md
@.planning/phases/01-foundation-skeleton/01-05-SUMMARY.md
@.planning/phases/01-foundation-skeleton/01-09-SUMMARY.md
</context>

## Artifacts this phase produces

本 plan 新建的符号（gap-closure 以修改既有符号为主，下面只列真正新增的）：

- **Rust 测试函数**：`settings_base_url_rejects_credential_bearing_values`
  （`crates/prism-store/src/settings.rs` 的 `mod tests` 内）
- **前端函数**：`localUrlIssue(raw: string): "invalid_url" | "invalid_url_credentials" | null`
  （`src/pages/Settings.tsx`，取代返回布尔的 `looksLikeHttpUrl`）
- **前端错误码键**：`ERROR_COPY["invalid_url_credentials"]`（`src/lib/ipc.ts`）
- **前端测试用例名**：`rejects a credential-bearing endpoint before it ever reaches the engine`
  （`src/pages/Settings.test.tsx`）
- **前端测试常量**：`CREDENTIAL_URL`（由既有 `FAKE_KEY` 拼出，见下方 fixture 决定）

**不新增**：`StoreError` 不加变体（它是 `#[non_exhaustive]`，`InvalidUrl` 语义完全够用，
加变体会连带要求 `src-tauri/src/commands.rs::map_err` 与 `ERROR_COPY` 同步扩表 —— 那是
IPC 短码契约的变更，不是本 gap 需要的东西）。engine 侧对两类拒绝一律回 `invalid_url` 短码；
「是凭据还是 scheme」的区分只在前端本地校验里做得到，因为那一层在 IPC 往返之前。

## Fixture 决定（PATTERNS.md 标记的冲突，在此拍板）

`crates/prism-llm/src/secrets.rs:103-106` 的 `FIXTURE_SECRET` 与
`src/pages/Settings.test.tsx:27-29` 的 `FAKE_KEY` 都带注释声明「刻意不长得像真密钥前缀，
以免 check-secrets.sh 把它当成明文密钥——那个扫描器不该为了迁就 fixture 而放宽」。
本 plan 需要一个「凭据形态的 URL」，与该约定表面冲突。

**拍板：URL fixture 的密码位复用既有的非 `sk-` fixture 值，不新建 allowlist。**

- Rust 侧用 `prism-test-secret-value`（与 `FIXTURE_SECRET` 同一串，但本文件内独立字面量，
  不跨 crate import——`prism-store` 不依赖 `prism-llm`，那条依赖方向由 check-deps.sh 守着）
- 前端侧直接复用既有的 `FAKE_KEY`，拼成 `` `https://u:${FAKE_KEY}@api.vendor.com/v1` ``

这两种形态都**完整地行使了守卫**（`url.password()` 返回 `Some(...)`，与真密钥无差别），
同时对 plan 01-11 widen 之后的扫描器仍然不命中（已实测逐条验证）。扫描器的 allowlist
一条都不新增——plan 01-11 甚至会把它对自身的排除也去掉。

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 值侧凭据守卫落在 engine 写入路径上（tracer：一条带凭据的 URL 走完 set_setting 全程被拒且不留痕）</name>
  <reversibility rating="reversible">拒绝 userinfo / query / fragment 是 `validate_base_url` 内的三行早退；放宽只需删掉它们。已存的 settings 值不会因此不可读（`get_setting` 不做校验，只有写路径校验），没有数据迁移，也没有对外契约变更。</reversibility>
  <files>crates/prism-store/src/settings.rs</files>
  <read_first>
    - `crates/prism-store/src/settings.rs`（被改文件本身）：`validate_base_url` 48-71 的早退写法要被逐字模仿；`set_setting` 88-104 上方那段「两道守卫都长在这里而不是调用方」的注释是本次改动必须保住的设计陈述；`mod tests` 111-137 已有 `fixture()` / `count()` / `count_key()` / `read()` 四个助手，复用、不要重定义
    - `crates/prism-store/src/error.rs`：`StoreError` 是 `#[non_exhaustive]`；`InvalidUrl(String)` 的文档注释写明「只描述规则，不回显传入的值」——新消息必须落在同一口径里
    - `.planning/phases/01-foundation-skeleton/01-PATTERNS.md` § `crates/prism-store/src/settings.rs` 一节：给出了 in-file analog 与 `settings_rejects_secret_like_keys`（206-234）的非恒真测试范式（拒绝断言 + 持久化断言 + 阴性对照三件套）
    - `.planning/phases/01-foundation-skeleton/01-VERIFICATION.md` § SC-4 证据：verifier 探针的四行输出，说明这条路径确实可复现而不是理论风险
    - `docs/keychain-naming.md`：不变量 1 的原文与它给出的理由（settings 会随数据库单目录整体备份被带走）
    - `crates/prism-store/tests/fts_cjk.rs` 文件头（1-8 行）：本 phase 的非恒真测试纪律——「每一条被删掉之后都会有一个具体的静默失败模式重新变得可能」
  </read_first>
  <behavior>
    `settings_base_url_rejects_credential_bearing_values`（单个测试函数，六组断言）：

    - ① 纯函数拒绝：下列四种输入 `validate_base_url` 均返回 `Err(StoreError::InvalidUrl(_))`
      · 用户名 + 密码：`https://u:prism-test-secret-value@api.vendor.com/v1`
      · 只有用户名：`https://prism-test-user@api.vendor.com/v1`
      · 非空 query：`https://api.vendor.com/v1?api-key=prism-test-secret-value`
      · 非空 fragment：`https://api.vendor.com/v1#prism-test-secret-value`
    - ② 写入路径拒绝（判别性所在——校验必须长在 `set_setting` 内，而不是指望调用方先调）：
      `store.write(|tx| set_setting(tx, SETTING_BASE_URL, 带凭据的那条))` 返回
      `Err(StoreError::InvalidUrl(_))`
    - ③ 未留痕：紧接 ② 之后 `count(&store) == 0`——被拒的值一行都没进表
    - ④ 不回显：①第一条的 `unwrap_err().to_string()` 里既不含用户名子串也不含密码子串
    - ⑤ 幂等（同一输入连写两次）：把 ② 再跑一遍，仍返回 Err，`count(&store)` 仍为 0——
      守卫无状态，第一次拒绝不会给第二次开门
    - ⑥ 阴性对照：`https://api.example.com/v1` 仍 `is_ok()`，且经 `set_setting` 写入后
      `count(&store) == 1`——守卫不是「一律拒绝」，若写成那样上面五条也都会绿

    既有的 `settings_base_url_validation` 与 `settings_rejects_secret_like_keys` 保持全绿，
    一个断言都不删。
  </behavior>
  <action>
    在 `crates/prism-store/src/settings.rs` 的 `validate_base_url` 中，**紧接现有 host 检查之后、
    明文 http 告警之前**，加入两段早退，形状与上面两段 `return Err(StoreError::InvalidUrl(...))`
    完全一致：

    第一段——凭据位。条件是 `!url.username().is_empty() || url.password().is_some()`。
    两个条件都要，因为 `https://user@host/v1` 这种只有用户名的形态里 `password()` 是 `None`，
    单看 password 会漏。消息保持 rule-shaped，形如「must not carry credentials in the userinfo
    component」——**禁止把 `raw`、`url.username()`、`url.password()` 中的任何一个插值进去**
    （T-01-26；`error.rs` 的 `InvalidUrl` 文档注释是这条纪律的权威出处）。

    第二段——query 与 fragment。条件是 `url.query().is_some() || url.fragment().is_some()`。
    理由写进注释：部分 OpenAI 兼容网关用 `?api-key=…` 形态传凭据，那是同一个洞的另一面；
    而 LLM 端点的 base_url 本身没有携带 query 或 fragment 的正当用途（真正的 query 由
    Phase 4 发请求时自己拼）。消息同样只描述规则。

    `StoreError` **不加新变体**，复用 `InvalidUrl`——它是 `#[non_exhaustive]` 的，加变体会
    连带要求 `src-tauri/src/commands.rs::map_err` 与前端 `ERROR_COPY` 同步扩表，而 IPC 短码
    契约的扩张不是本 gap 需要的东西（前端要的区分在本地校验层就能做到，见 Task 2）。

    `set_setting` 本身**不改**——它已经在 `key == SETTING_BASE_URL` 时调用 `validate_base_url`，
    守卫长在写入路径上这件事本来就成立，缺的只是守卫的覆盖面。这一点很重要：改动落在被调用的
    校验函数里，而不是新加一个调用点，否则「机制而非约定」那条设计陈述就被破坏了。

    在同文件 `mod tests` 里按 `<behavior>` 新增 `settings_base_url_rejects_credential_bearing_values`，
    复用已有的 `fixture()` / `count()` 助手。测试注释里逐条写明「这一条被删掉之后，哪个静默失败
    模式会重新变得可能」——照 `tests/fts_cjk.rs` 文件头与 `settings_rejects_secret_like_keys`
    的写法。fixture 字符串按本 plan 上方「Fixture 决定」一节选取，不要另造 `sk-` 开头的串。
  </action>
  <verify>
    <automated>cargo test -p prism-store settings &amp;&amp; cargo clippy -p prism-store -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p prism-store settings_base_url_rejects_credential_bearing_values` 退出 0
    - `cargo test -p prism-store settings` 退出 0，且输出显示三个 `settings_*` 测试函数全部执行
      （新测试 + `settings_base_url_validation` + `settings_rejects_secret_like_keys` + `settings_roundtrip`）
    - `cargo test -p prism-store` 整体退出 0（迁移 / 并发 / fts_cjk 一条都没被带红）
    - `cargo clippy -p prism-store -- -D warnings` 退出 0
    - **反证 A（守卫非恒真，落点唯一）**：临时删掉 `validate_base_url` 里的凭据位早退分支后重跑，
      `settings_base_url_rejects_credential_bearing_values` 变红，且失败断言指向 ① 的第一条或 ②；
      撤销后转绿
    - **反证 B（query/fragment 分支独立成立）**：临时删掉 query/fragment 早退分支后重跑，
      同一测试变红且失败断言指向 ① 的第三或第四条；撤销后转绿
    - **反证 C（阴性对照有效）**：临时把 `validate_base_url` 改成对所有输入返回 Err 后重跑，
      同一测试变红且失败断言指向 ⑥；撤销后转绿——证明这组用例挡得住「一律拒绝」式的假修复
    - **端到端复现已消失**：用 verifier 的原始探针形态手工确认一次——
      `store.write(|tx| set_setting(tx, "llm.base_url", "https://user:prism-test-secret-value@api.vendor.com/v1"))`
      返回 Err，且 `SELECT value FROM settings WHERE key='llm.base_url'` 无行
      （该断言已被 ②③ 覆盖，此处只是与 01-VERIFICATION.md 的探针输出逐行对照）
    - 源码断言：`validate_base_url` 函数体内出现 `url.password()` 与 `url.username()`，
      且这两个调用的返回值**没有**出现在任何 `format!` / 消息构造里
    - `bash scripts/check-deps.sh all` 仍退出 0（本 task 不引入任何新依赖）
  </acceptance_criteria>
  <done>凭据型 base_url 在 engine 写入路径上被拒、不进表、错误不回显值，且三条反证证明这组断言不是恒真的。01-VERIFICATION.md gap 1 的 engine 半边关闭。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 前端本地校验同步收紧，并把「凭据」与「scheme 不对」分成两条文案</name>
  <reversibility rating="reversible">前端这一层被自己的注释明确定义为体验层而非安全边界；放宽它只影响「多久才知道写错了」，权威校验在 engine 侧且已由 Task 1 守住。</reversibility>
  <files>src/pages/Settings.tsx, src/lib/ipc.ts, src/pages/Settings.test.tsx</files>
  <read_first>
    - `src/pages/Settings.tsx` 14-20（`looksLikeHttpUrl` 与它上方那段「**这不是安全边界**——绕过界面直接 invoke 就没有它；权威校验长在 engine 的 `set_setting` 写入路径上」的注释，这段必须原意保留）与 80-87（`submitUrl` 的调用点）
    - `src/lib/ipc.ts` 36-68：`ERROR_COPY` 表与 `errorCopy` 的契约——错误码是稳定短串、只给代码分支用，中文文案只在这张表里生成，码串本身从不进 DOM。新文案必须加成表里的一个**新键**，不要在组件里内联字面量（本仓库没有任何组件自带错误文案）
    - `src/pages/Settings.test.tsx` 6-55（`vi.mock("../lib/ipc")` 的 `importOriginal` 展开、`afterEach(cleanup)`、每测试 `mockReset()` 循环——扩展既有 harness，不要另建一套）、27-29（`FAKE_KEY` 与它的 fixture 命名约定注释）、72-86（`never echoes the typed key back into the DOM` 的两条断言缺一不可范式）、102-128（base_url 的两条既有用例）
    - `.planning/phases/01-foundation-skeleton/01-PATTERNS.md` § `src/pages/Settings.tsx` 与 § `src/pages/Settings.test.tsx` 两节
    - 本 PLAN 上方的「Fixture 决定」一节
  </read_first>
  <behavior>
    新测试 `rejects a credential-bearing endpoint before it ever reaches the engine`
    （放进既有的 `describe("SettingsPage / base_url", …)` 里）：

    - ① 在端点输入框填入 `` `https://u:${FAKE_KEY}@api.vendor.com/v1` `` 并点「保存端点」后，
      `role="alert"` 出现，文案中含「用户名」或「密码」字样（中文规则文案，不是原始码串）
    - ② `expect(setBaseUrl).not.toHaveBeenCalled()`——本地就拦下了，一次 IPC 都没发出去。
      ①② 缺一不可：只有 ① 时，一个**根本没提交表单**的组件也会通过；只有 ② 时，一个
      「按钮坏了」的组件也会通过（照 72-80 的两条断言范式）
    - ③ 文案不回显：`document.body.innerHTML` 中不含 `FAKE_KEY` 那段子串
    - ④ 阴性对照（同一测试内）：清空后改填 `https://api.example.com/v1` 再点保存，
      `setBaseUrl` 被以该串调用一次——证明这条守卫不是「一律拒绝」

    既有五条用例（含 `renders the engine's rejection as Chinese copy and no success notice`
    与 `saves an http(s) endpoint and reports success`）全部保持绿，一条都不改。
  </behavior>
  <action>
    **`src/lib/ipc.ts`**：在 `ERROR_COPY` 里 `invalid_url` 那条旁边新增一个键
    `invalid_url_credentials`，中文文案要说清两件事：链接里不能带用户名/密码
    （形如 `user:pass@host`）也不能带查询串或锚点；密钥请填在上面的 API key 栏，
    它只进系统钥匙串、不入数据库。文案保持 rule-shaped——**不得回显 `urlDraft`**。
    `errorCopy` 的实现一个字不动（它的原型链问题是 WR-05，本次范围之外）。

    **`src/pages/Settings.tsx`**：把 `looksLikeHttpUrl` 换成
    `localUrlIssue(raw: string): "invalid_url" | "invalid_url_credentials" | null`
    （返回 `null` 表示本地看不出问题）。判定顺序：
    先 `trim()`，再要求以 `http://` 或 `https://` 开头（否则 `"invalid_url"`）；
    然后 `new URL(trimmed)` 放进 try/catch，解析失败回 `"invalid_url"`；
    解析成功后若 `username !== ""` 或 `password !== ""` 或 `search !== ""` 或 `hash !== ""`
    则回 `"invalid_url_credentials"`；其余回 `null`。
    判定项与 engine 侧 `validate_base_url` **逐项对齐**（scheme / userinfo / query / fragment），
    这样「前端放过但 engine 拒绝」的情况不会在正常输入上出现。
    函数上方那段「**这不是安全边界**——绕过界面直接 invoke 就没有它，权威校验长在 engine 的
    `set_setting` 写入路径上」的注释保留并补一句：这一层现在多认一种形态，是为了让用户
    在按下保存之前就知道那串东西不该填在这里，而不是因为它变成了防线。

    `submitUrl()` 改为取 `localUrlIssue(urlDraft)` 的返回值：非 `null` 时
    `setUrlNotice({ tone: "error", text: errorCopy(issue) })` 并 return；`null` 时照旧
    `saveUrl.mutate(urlDraft.trim())`。

    **`src/pages/Settings.test.tsx`**：在既有 `describe("SettingsPage / base_url", …)` 内按
    `<behavior>` 新增一条用例。凭据 URL 用模板串由既有 `FAKE_KEY` 拼出（见「Fixture 决定」），
    **不要**新造 `sk-` 开头的字面量，也不要动 27-29 那段 fixture 命名约定的注释。
  </action>
  <verify>
    <automated>npm run test -- --run src/pages/Settings.test.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/pages/Settings.test.tsx` 退出 0，且该文件的用例数为 8
      （既有 7 条 + 新增 1 条）
    - `npm run test -- --run` 整体退出 0（既有 32 条一条都没被带红）
    - `npx tsc --noEmit` 退出 0
    - `npm run build` 退出 0
    - **反证 D（前端守卫非恒真）**：临时把 `localUrlIssue` 里的 userinfo/query/fragment 判定删掉
      （只留 scheme 判定）后重跑，新用例变红且失败断言指向 ① 或 ②；撤销后转绿
    - **反证 E（阴性对照有效）**：临时把 `localUrlIssue` 改成对所有输入返回 `"invalid_url_credentials"`
      后重跑，新用例变红且失败断言指向 ④；撤销后转绿
    - 源码断言：`src/pages/Settings.tsx` 中不含任何面向用户的中文错误字面量——
      端点相关的所有错误文案都经 `errorCopy(...)` 取自 `src/lib/ipc.ts` 的 `ERROR_COPY`
    - 源码断言：`src/lib/ipc.ts` 的 `ERROR_COPY` 里 `invalid_url_credentials` 键存在，
      且 `errorCopy` 函数体未被修改（与 `git diff` 对照，该函数零行变更）
    - 行为断言（与 Task 1 的对齐性）：`localUrlIssue` 判定的四类问题
      （scheme / userinfo / query / fragment）与 `validate_base_url` 拒绝的四类完全同集——
      两侧任一多认或少认一类，都会让「前端放行、engine 拒绝」在正常输入上重新可能
  </acceptance_criteria>
  <done>前端在提交前就拦下带凭据的端点、给出专属中文文案且不回显输入，阴性对照证明它不是「一律拒绝」；两道守卫的判定面对齐。01-VERIFICATION.md gap 1 全部关闭。</done>
</task>

</tasks>

## Probe Coverage & Flagged Assumptions（全 plan 集共用的账，记在本 lead plan）

specless 边界探针给出 9 条 applicable edge，全部 `unresolved`。按 §A/§C 逐条处置，
**无静默丢弃**：9 == 4（authored as covered truths）+ 2（authored as backstop markers）
+ 3（flagged assumptions）。

| # | Requirement | Category | 处置 | 落点 |
|---|---|---|---|---|
| 1 | INFRA-01 | boundary | covered | plan 01-11 truth：`sk-` 后 20 字符命中 / 19 字符不命中的成对样本 |
| 2 | INFRA-01 | adjacency | covered | plan 01-12 truth：`constant_time_eq("", "")` 为 false 而等长同值为 true |
| 3 | INFRA-01 | empty | covered | plan 01-12 truth：`McpDeps::new(…, "")` 返回 Err |
| 4 | INFRA-01 | ordering | **flagged assumption** | 本 gap 集没有任何排序/相等元素输出面（守卫是拒/放二值，扫描器输出顺序无人消费）。不为它编造验收项。 |
| 5 | INFRA-01 | precision | **flagged assumption** | 本 gap 集没有数值面（无浮点、无取整、无溢出边界）。不为它编造验收项。 |
| 6 | INFRA-01 | concurrency | backstop | plan 01-13 truth marker：两线程并发调用 `init_tracing()` 时至多一个安装成功 |
| 7 | INFRA-02 | unclassified | **flagged assumption** | 探针未能归类，按 §A 保持 `unresolved`、不自动 backstop、不自动 dismiss。人工判读：INFRA-02（SQLite/FTS 骨架）在本次 gap 集里只被间接触及（settings 写入事务），没有独立的未覆盖边界。 |
| 8 | INFRA-03 | idempotency | covered | 本 plan truth：同一凭据 URL 连写两次都被拒、表行数始终 0 |
| 9 | INFRA-03 | concurrency | backstop | 本 plan truth marker：并发写入下守卫仍在单写者事务内先于 `tx.execute` 生效 |

两条 `backstop` 在 verify 时若拿不到明确证据将 abstain → `human_needed`，不会静默通过。
三条 flagged assumption 在此显式登记，供 `/gsd-verify-work` 判读，**不视为已覆盖**。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户在设置页输入的字符串 → `set_base_url` 命令 → engine `set_setting` | 未经校验的任意串在此进入持久化路径 |
| SQLite sidecar 库（`~/Library/Application Support/PrismDocs/`）→ 用户的备份介质 | INFRA-05 要求「数据库单目录整体备份可恢复」，因此表里的一切都会离开本机 |
| engine 错误文本 → IPC → 前端 DOM / 本地日志 | 校验失败的消息一路冒泡，被误填的密钥可能随消息外泄 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-39 | Information Disclosure | `settings.value` 承载凭据型 `base_url`（`https://user:key@host/v1`），随 sidecar 单目录备份离开本机 | **critical** | mitigate | `validate_base_url` 在写入路径上拒绝非空 `username` / 非 `None` `password`；测试断言被拒的值 `count(*) == 0`，并配「一律拒绝」的阴性对照证明守卫有判别性 |
| T-01-40 | Information Disclosure | `?api-key=…` / `#…` 形态的网关凭据经 query / fragment 绕过 userinfo 检查 | high | mitigate | 同一函数内第二段早退拒绝非空 `query()` 与 `fragment()`；LLM base_url 无携带二者的正当用途，真正的 query 由 Phase 4 发请求时自拼 |
| T-01-41 | Information Disclosure | 校验失败的错误消息回显被拒的 value（很可能就是密钥本身），经 IPC 进入 DOM 与本地日志 | medium | mitigate | 新消息全部 rule-shaped，禁止插值 `raw` / `username()` / `password()`；测试断言错误串中不含用户名与密码子串（T-01-26 同源） |
| T-01-42 | Spoofing | 前端本地校验与 engine 校验判定面不一致，用户看到「已保存」而 engine 实际拒绝（或反之） | low | mitigate | `localUrlIssue` 与 `validate_base_url` 的四类判定逐项对齐并写入验收项；前端层被注释明确定义为体验层，权威始终在 engine |
| T-01-43 | Elevation of Privilege | 绕过界面直接 `invoke('set_base_url', …)` 提交凭据型 URL | high | mitigate | 守卫长在 `set_setting` 内部而非调用方——绕过 UI 不改变结果；这正是 `set_setting` 88-104 注释所述「放调用方是约定，放写入路径才是机制」 |
</threat_model>

<verification>
- `cargo test -p prism-store` 全绿；`cargo clippy -p prism-store -- -D warnings` 退出 0
- `npm run test -- --run` 全绿；`npx tsc --noEmit` 与 `npm run build` 退出 0
- 五条反证（A engine userinfo 分支、B engine query/fragment 分支、C engine 一律拒绝、
  D 前端判定、E 前端一律拒绝）各自使对应测试变红，且落点是被守的那条断言而非前置条件
- `bash scripts/check-deps.sh all` 与 `bash scripts/check-secrets.sh` 退出 0
- 与 01-VERIFICATION.md § SC-4 的探针输出逐行对照：`PROBE validate_base_url` 与
  `PROBE set_setting` 两行现在都应为 `false`（被拒），`PROBE stored value` 为 `None`
</verification>

<success_criteria>
成功标准 4 第三分句「代码与配置中无明文密钥」的**写入侧**恢复成立：密钥容器的边界从键名
扩到值，`docs/keychain-naming.md` 不变量 1 在 `llm.base_url` 这条路径上重新为真。
INFRA-03 的这一半解除阻塞（另一半——扫描器能否看见明文密钥——由 plan 01-11 关闭）。
</success_criteria>

<output>
Create `.planning/phases/01-foundation-skeleton/01-10-SUMMARY.md` when done
</output>
