---
phase: 01-foundation-skeleton
plan: 28
type: execute
wave: 6
depends_on: ["01-27"]
files_modified:
  - rustfmt.toml
  - .github/workflows/ci.yml
  - justfile
  - .planning/phases/01-foundation-skeleton/deferred-items.md
autonomous: false
gap_closure: true
requirements: [INFRA-01]
tags: [rustfmt, toolchain, decision, phase-closure]

must_haves:
  truths:
    - "rustfmt 的取向已由用户在 checkpoint 上拍板，并按所选方向落地"
    - "若选方向 A：`cargo fmt --all -- --check` 退出 0，且 CI engine job 的第一步就是它"
    - "若选方向 A：格式化之后全部源码序与 `include_str!` 断言仍绿——任何因锚点被拆行而变红的断言，修的是**锚点**而不是断言本身"
    - "若选方向 B：`justfile` 文件头含明确的「本项目不采用 rustfmt 默认风格」决定记录（含日期与理由），且仓库里没有 `rustfmt.toml`"
    - "无论选哪个方向：`cargo test --workspace` / `cargo clippy --workspace --all-targets -- -D warnings` / `npm run test -- --run` 全绿，`check-deps.sh all` 与 `check-secrets.sh all` 都退出 0"
    - "`deferred-items.md` 里 rustfmt 那条的「去向」小节已回填最终结论"
    - statement: "本 plan 不放宽任何既有闸门；若某条源码序断言因格式化而变红，只允许更换锚点片段，禁止删除或放宽该断言"
      verification: backstop
  artifacts:
    - "rustfmt.toml —— 仅在方向 A 下创建；空文件即代表显式采用 rustfmt 默认风格"
    - ".github/workflows/ci.yml —— 仅在方向 A 下新增 fmt 步骤（engine job 最前）"
    - "justfile —— 方向 A 下新增 `fmt-check` recipe；方向 B 下在文件头记录决定"
    - ".planning/phases/01-foundation-skeleton/deferred-items.md —— rustfmt 条目的最终结论"
  key_links:
    - "`cargo fmt --all` → 全部 `include_str!` 源码序断言（`open.rs` / `lib.rs` / `commands.rs` / `services.rs` / `middleware.rs`）—— 格式化可能打断它们的锚点片段"
    - "`cargo fmt --all` → `scripts/check-secrets.sh scan` 的匹配面（换行变化可能让某个字面量跨行或并行）"
    - "本 plan 的决策 → `deferred-items.md` 里 01-25 写下的「去向」小节（必须回填）"
  prohibitions:
    - "第三方包在未经合法性核对前不得进入依赖树"
    - "用户文档正文与评论正文不得进入本地日志、错误文本或 MCP 响应正文"
    - "密钥（LLM API key / MCP bearer token）不得进入 SQLite、日志、错误文本或前端 DOM"
    - "文档内容不得发送到用户配置的 LLM 端点之外的任何网络目的地"
    - "拒绝响应不得透露请求在哪一层被拒（状态码、正文、时序）"
    - "埋点数据不得在未经用户 opt-in 的情况下离开本机"
---

<objective>
Phase 1 gap-closure 的最后一份 plan。关闭 `deferred-items.md` 里从 plan 01-03 悬到现在的那条：`cargo fmt --all -- --check` 全仓有差异且 CI 无 fmt 闸门。

用户要求「二选一，别继续悬着」——本 plan 用一个 `checkpoint:decision` 请用户拍板，随后按所选方向落地。

**为什么它单独成一份 plan 且排在最后**：方向 A 的一次性格式化会触及**每一个** Rust 文件，与本轮任何其他 plan 并行都会抢文件；而它同时有一个具体风险——本仓库多条 `include_str!` 源码序断言靠**字符串片段**定位，rustfmt 的换行偏好可能把某个锚点拆到两行，使断言在实现完全正确时变红。这不是不做的理由，而是做完必须立刻全量重跑并逐条核对的理由。

本 plan 同时汇总 Phase 1 收尾的两项**人工验证**（见 `<outstanding_human_verification>`），步骤已按本轮改动更新（01-24 的两条新 CSP 指令、01-21 的 `RUST_LOG` 上限与降档 warn、01-21 的 release dev 命令分叉）。

Output: 一条被拍板并落地的 rustfmt 取向，加一份更新过的人工验证清单。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@justfile
@.github/workflows/ci.yml
@.planning/phases/01-foundation-skeleton/deferred-items.md
@.planning/phases/01-foundation-skeleton/01-VERIFICATION.md
</context>

<why_no_tracer>
缺口修补集，无新功能垂直切片可领；本轮按主题横向聚类，`TRACER_MODE` 不适用（理由同 01-14）。
</why_no_tracer>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: rustfmt 取向二选一</name>
  <read_first>
    - `.planning/phases/01-foundation-skeleton/deferred-items.md` 第 6-17 行（这条候选的原始登记：差异全部为 rustfmt 的换行偏好、非正确性问题；差异早于 plan 01-03；建议在收尾时一次性拍板以避免逐 plan 各自格式化造成风格拉锯）以及 01-25 为它写下的「去向」小节
    - 本机实跑 `cargo fmt --all -- --check` 的当前输出 —— 把受影响的文件清单与差异行数作为决策材料呈给用户，不要凭印象描述规模
  </read_first>
  <decision>Phase 1 收尾时对 rustfmt 的取向：采用 rustfmt 默认风格并加 CI 闸门（一次性格式化全仓），还是明确记录为不采用、不加闸门</decision>
  <context>
    这条从 plan 01-03 登记至今一直悬着。`cargo fmt --all -- --check` 在多处报差异（全部是换行偏好，无正确性问题），而 CI 只跑 `check-deps` / `check-secrets` / `clippy` / `test` / `lint`，**没有 fmt 步骤**，所以这些差异不会让 CI 变红。

    每多一个 phase 就多一批文件，且逐 plan 各自格式化会造成风格拉锯。

    **可逆性：`costly`（不是 one-way）。** 两个方向都是一次全仓 diff，没有已发布契约被破坏、不需要数据迁移。但方向 A 会产生一个触及大量文件的提交，且此后每个 phase 都受它约束。

    **方向 A 的一个具体风险必须先说清**：本仓库有多条 `include_str!` 源码序断言，它们靠**字符串片段**定位 —— `open.rs` 的 `to_latest(&mut writer)` 与 `Pool::builder()`、`lib.rs` 的 `let _ = init_tracing();` 与 `tauri::Builder::default()`（以及 01-21 新增的 release 命令列表断言）、`commands.rs` 与 `services.rs` 的 `production_source()` 式哨兵（01-20 给后者加了校验早于日志的顺序断言）、`middleware.rs` 的 `the_comparison_is_not_a_plain_equality`（01-16 给它加了第三条断言）。rustfmt 的换行偏好可能把某个锚点拆到两行，使断言在实现完全正确时变红——本 phase 已实测踩过一次同类问题（锚点写成裸名字会命中解释性注释而假红）。
  </context>
  <options>
    <option id="option-a">
      <name>A：采用 rustfmt 默认风格 + CI fmt 闸门 + 一次性格式化全仓</name>
      <pros>
        - 风格问题从此不再需要人讨论，每次 push 自动裁决
        - 现在做只涉及本 phase 建立的文件；每晚一个 phase 成本都更高
        - 显式化：建一个（可以为空的）`rustfmt.toml` 表示「采用默认风格是一个决定，不是没人管」
      </pros>
      <cons>
        - 一次触及大量文件的提交，`git blame` 在这些行上会指向格式化提交
        - 可能打断多条 `include_str!` 源码序断言的锚点片段，必须逐条重跑并修复
        - 对已经写好的注释排版可能有非预期观感改动（rustfmt 默认不重排注释，但代码换行会带动上下文）
      </cons>
    </option>
    <option id="option-b">
      <name>B：明确记录为不采用 rustfmt 默认风格，不加闸门</name>
      <pros>
        - 零改动，零风险；不产生大提交，不打断任何源码序断言
        - 本项目的注释密度很高且刻意排版，默认风格未必更好
      </pros>
      <cons>
        - 风格差异此后靠人；每次代码评审多一类噪声
        - 与「断言而非约定」的项目基调不一致——这会是本仓库唯一一处刻意选择靠约定的地方
        - 差异会随 phase 累积，将来若改主意成本更高
      </cons>
    </option>
  </options>
  <acceptance_criteria>
    - 用户明确选定 `option-a` 或 `option-b` 之后才进入 Task 2
    - 决策材料里已包含本机 `cargo fmt --all -- --check` 的**实际输出**（受影响文件清单 + 差异规模），不是凭印象描述
    - 决策与理由记录进 SUMMARY
  </acceptance_criteria>
  <reversibility rating="costly">两个方向都是一次全仓 diff；无已发布契约被破坏、无数据迁移，但方向 A 此后约束每个 phase 且会重写大量行的 blame。</reversibility>
  <resume-signal>选择：option-a 或 option-b</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 按决策落地 rustfmt 取向并回填 deferred-items</name>
  <files>rustfmt.toml, .github/workflows/ci.yml, justfile, .planning/phases/01-foundation-skeleton/deferred-items.md</files>
  <read_first>
    - Task 1 的决策结论
    - `crates/prism-store/src/open.rs` `mod tests` 的源码序断言（锚点 `to_latest(&mut writer)` / `Pool::builder()`）
    - `src-tauri/src/lib.rs` `mod tests` 的源码序断言（锚点 `let _ = init_tracing();` / `tauri::Builder::default()`，以及 01-21 新增的 release 命令列表断言）
    - `src-tauri/src/commands.rs` 的 `production_source()` 与它的两条哨兵
    - `crates/prism-engine/src/services.rs` 的 `the_service_impls_contain_no_await` 与 01-20 新增的「校验早于日志」顺序断言
    - `crates/prism-mcp/src/middleware.rs` 的 `the_comparison_is_not_a_plain_equality`（01-16 新增了第三条断言）
    - `.github/workflows/ci.yml` engine job 的步骤顺序（fmt 若加，应排在最前——它最便宜）
    - `.planning/phases/01-foundation-skeleton/deferred-items.md` 里 01-25 写下的「去向」小节（本 Task 要回填最终结论）
  </read_first>
  <action>
    **若选 option-a**：
    1. 在仓库根建 `rustfmt.toml`。内容可以为空（或只含一条 `edition = "2021"`），文件头注释写明：空文件即代表「显式采用 rustfmt 默认风格」这个**决定**，而不是「没人配置过」。
    2. 跑 `cargo fmt --all`，把全仓格式化一次。
    3. **立刻**全量重跑：`cargo test --workspace`、`cargo clippy --workspace --all-targets -- -D warnings`、01-27 扩面后的两条 clippy 命令、`cargo test -p prismdocs-shell --features test`。逐条检查上面 `read_first` 里列出的**全部**源码序断言是否仍绿。任何因此变红的断言，修的是**锚点**（换一个 rustfmt 不会拆开的、仍然只可能命中目标语句的片段），**不是**把断言删掉或放宽。每处修改都要在 SUMMARY 里记录旧锚点、新锚点与选择理由。
    4. 重跑 `bash scripts/check-secrets.sh all` —— 换行变化可能让某个字面量跨行（漏检）或让两段并到一行（新命中）。若出现新命中，按 `check-secrets.sh` 自述的单向约定处理（改 fixture 不改防线）。
    5. 在 `.github/workflows/ci.yml` 的 engine job **最前面**加一步 `cargo fmt --all -- --check`（它最便宜，先红的应当是最早那一层）。`dtolnay/rust-toolchain@stable` 默认带 rustfmt；若不带，在 `components` 里补上。
    6. 在 `justfile` 加一条等价的 `fmt-check` recipe（单行委托，不重复实现）。

    **若选 option-b**：
    1. **不**建 `rustfmt.toml`，**不**跑 `cargo fmt --all`，**不**加 CI 步骤。
    2. 在 `justfile` 顶部的文件头注释里加一段，写明这是一个被明确做出的决定：本项目不采用 rustfmt 默认风格，不设 fmt 闸门，风格问题在代码评审里处理。写清做这个决定的日期与理由，使下一个读者不会把它当成遗漏——这正是本轮反复出现的那个教训：一个被想过之后接受的取舍与一个没人注意到的空洞，在文件里长得一样。

    **两个方向都要做的最后一步**：把最终结论回填 `.planning/phases/01-foundation-skeleton/deferred-items.md` 里 01-25 写下的「去向（Phase 1 收尾定案）」小节——用 `Edit` 追加结论行，不要重写该文件、不要删除原有登记内容。
  </action>
  <verify>
    <automated>cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && bash scripts/check-deps.sh all && bash scripts/check-secrets.sh all</automated>
  </verify>
  <acceptance_criteria>
    - 落地方向与 Task 1 的决策一致（SUMMARY 里明确写出选了哪个）
    - `cargo test --workspace` 全绿；`cargo clippy --workspace --all-targets -- -D warnings` 0 warning
    - `cargo test -p prismdocs-shell --features test` 全绿；`npm run test -- --run` 全绿；`npx tsc --noEmit` 0 error
    - `bash scripts/check-deps.sh all` 与 `bash scripts/check-secrets.sh all` 都退出 0
    - **若选 option-a**：`cargo fmt --all -- --check` 退出 0；`read_first` 里列出的全部源码序断言逐条确认仍绿（在 SUMMARY 里列出清单与逐条结论）；任何被修改的锚点都记录了旧值、新值与理由
    - **若选 option-a 的非恒真反证（必须实跑）**：临时把某个文件改成明显不符合 rustfmt 默认风格的排版 → `cargo fmt --all -- --check` 退出 1；还原后退出 0。输出抄进 SUMMARY
    - **若选 option-b**：`justfile` 文件头含明确的「本项目不采用 rustfmt 默认风格」决定记录（含日期与理由），且 `ls rustfmt.toml` 报文件不存在
    - `.planning/phases/01-foundation-skeleton/deferred-items.md` 的对应「去向」小节已回填最终结论，且 `git diff --stat` 对该文件的删除计数为 0
  </acceptance_criteria>
  <done>rustfmt 的取向不再悬着，且无论选哪个方向，这个决定都被写在一个下一个读者会看到的地方。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cargo fmt --all` → `include_str!` 源码序断言 | 格式化可能打断锚点片段，使断言在实现正确时假红——而假红最容易被「顺手删掉」解决 |
| `cargo fmt --all` → `check-secrets.sh` 的匹配面 | 换行变化可能让某个字面量跨行（漏检）或并行（新命中） |
| 一个未被记录的决定 → 下一个读者 | 一个被想过之后接受的取舍与一个没人注意到的空洞，在文件里长得一样 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01G-63 | Tampering | 全仓格式化打断源码序断言使其假红或被顺手删掉 | high | mitigate | Task 2：格式化后逐条重跑全部源码序断言；变红时**修锚点而非删断言**；每处改动记录旧/新锚点 |
| T-01G-64 | Information Disclosure | 格式化后 `check-secrets.sh` 的字面量跨行导致漏检 | medium | mitigate | Task 2 第 4 步：重跑 `check-secrets.sh all`；新命中按单向约定改 fixture |
| T-01G-65 | Repudiation | 决定未被记录，下一个读者当成遗漏 | low | mitigate | 两个方向都要求就地记录决定（`rustfmt.toml` 文件头或 `justfile` 文件头）+ 回填 `deferred-items.md` |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | 本 plan 不新增任何包（rustfmt 是 rustup 组件，非第三方包；本轮唯一的包引入面已由 01-26 的 blocking-human 闸门关闭） |
</threat_model>

<verification>
```bash
# Rust 全量
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo clippy -p prismdocs-shell --all-targets --features test -- -D warnings
cargo test -p prismdocs-shell --features test

# 断言脚本
bash scripts/check-deps.sh all
bash scripts/check-secrets.sh all

# 前端
npm run lint
npx tsc --noEmit
npm run test -- --run
npm run build

# 仅 option-a
cargo fmt --all -- --check
```
</verification>

<success_criteria>
- rustfmt 取向已拍板并落地，且这个决定被写在下一个读者会看到的地方
- 若全仓格式化，全部源码序断言逐条确认仍绿，无一条被删或放宽
- `deferred-items.md` 的 rustfmt 条目有最终结论
- 全部既有闸门在本 plan 之后仍然退出 0
</success_criteria>

<artifacts_this_phase_produces>
| 类别 | 名称 | 位置 |
|---|---|---|
| 文件（新，仅 option-a） | `rustfmt.toml`（可为空；空即代表显式采用默认风格） | 仓库根 |
| CI 步骤（新，仅 option-a） | `cargo fmt --all -- --check`（engine job 最前） | `.github/workflows/ci.yml` |
| justfile recipe（新，仅 option-a） | `fmt-check` | `justfile` |
| justfile 文件头（改，仅 option-b） | 「本项目不采用 rustfmt 默认风格」的决定记录（含日期与理由） | `justfile` |
| 结论行（新） | `deferred-items.md` rustfmt 条目「去向」小节的最终结论 | `.planning/phases/01-foundation-skeleton/deferred-items.md` |

无新增 npm/cargo 包。option-a 下会有大量 `.rs` 文件被格式化——它们不是新符号，只是排版变化。
</artifacts_this_phase_produces>

<outstanding_human_verification>
以下两项**不可自动判定**，不是任务。本 plan 作为 Phase 1 gap-closure 的最后一份，把它们汇总为收尾的人工验证清单。步骤已按本轮改动更新：01-24 给两份 CSP 加了 `form-action` / `frame-ancestors`；01-21 给 `RUST_LOG` 加了项目上限与降档 warn，并把四个 `dev_*` 命令移出 release 的 `generate_handler!`。

### 1. 真实 WebView 下的 CSP 与 IPC 双通路（同时承载成功标准 2 的复验）

**Test:**
1. `npm run tauri dev` 起应用 → 窗口**不是白屏**
2. 打开设置页：状态行、两个输入框、两个按钮都在；随手保存一个合法端点（形如 `https://api.anthropic.com`），确认成功文案出现
3. 打开 dev 冒烟页（右下角 dev 开关），跑三个验证入口：
   - **总线事件往返**：点一次，事件计数 +1；离开页面再回来再点，计数仍与点击次数 **1:1**（不翻倍）
   - **Channel 有序流**：点一次，读数应为「seq 校验通过 · 实收 1000 条」
   - **中文搜索**：先「写入样例文档」，再搜「锚定引擎」应命中 >0；搜「量子纠缠」应为 0（阴性对照）
4. 打开 WebView 控制台，确认**无任何 CSP 违规报告**——本轮新加了 `form-action 'none'` 与 `frame-ancestors 'none'`，要特别留意这两条是否在设置页或冒烟页触发违规
5. `npm run tauri build` 出 dmg，安装后对装出来的 app **重复第 1–4 步**（发布形态走 `csp` 而不是 `devCsp`，这是验证严格那一份的唯一路径）
6. **发布形态额外确认**（01-21 新增）：dev 冒烟页的开关按钮不存在（`import.meta.env.DEV` 摇掉），且四个 `dev_*` 命令已移出 release 的 `generate_handler!`——若发布形态下仍能触达冒烟页且命令仍能调用，说明分叉未生效

**Expected:** 六步全部正常。三个入口的读数与 01-09 人工验证时一致（事件计数 1:1、「seq 校验通过 · 实收 1000 条」、「锚定引擎」命中 >0 且「量子纠缠」= 0）。

**Why human:** CSP 只在真实 WebView 里生效——jsdom（`tauri-security.test.ts` 第 17 行自己写明）与 `cargo test`（走 `mock_builder`，无 WebView）都结构性看不见它。**且这一项同时是 SC-2 的复验**：SC-2 的真实 WebView 证据取自 `csp: null` 的环境，而 `connect-src 'self' ipc: http://ipc.localhost` 直接管辖 IPC 来源；若它在真实 WebView 下挡住 IPC，受影响的不是某一个命令而是**全部十个**。旧读数不得沿用。

**出现违规时的处理：** 只放宽 `devCsp`，或按控制台报告点名的指令逐项追加到 `csp`；**禁止**把 `csp` 设回 `null`，也禁止直接删掉本轮新加的两条指令。若确需放宽，先在 `src/lib/tauri-security.test.ts` 的精确相等断言上过一次评审——那正是 01-24 让它变成精确相等的意义。

### 2. 日志 sink 真的有落点

**Test:**
1. 在 `npm run tauri dev` 的终端里，打开设置页把 base_url 设成一个**非 loopback 的 `http://`** 端点（例如 `http://example.com/v1`），点保存
2. 观察终端
3. **本轮新增的第二半**（因 01-21 的 `RUST_LOG` 上限而增加）：用 `RUST_LOG=trace npm run tauri dev` 再起一次，观察终端

**Expected:**
- 第 2 步：出现 tracing 格式的行，且 `crates/prism-store/src/settings.rs` 那条明文 http 告警（`LLM endpoint uses plaintext http to a non-loopback host`）**实际打出来**
- 第 3 步：出现 01-21 新增的**降档 warn**（说明环境提供的档位超过项目上限）；终端里**没有**大量 `rmcp` target 的 trace 行（Phase 1 尚未启动 MCP server，因此更实际的判据是「降档 warn 出现」）

**Why human:** `tracing::dispatcher::has_been_set()` 只证明 dispatcher 就位，不证明日志到达终端（EnvFilter 档位、fmt 层的输出目标都可能让它落空）。三条安全决策日志（无差别 403 的真实原因、明文 http 告警、钥匙串不可用降级提示）是否真有落点，取决于这条端到端确认。
</outstanding_human_verification>

<output>
Create `.planning/phases/01-foundation-skeleton/01-28-SUMMARY.md` when done.

SUMMARY 必须包含：
1. Task 1 的决策结论与理由，以及回填 `deferred-items.md` 的确认
2. （若 option-a）全仓格式化后逐条源码序断言的重跑结论，含任何被修改的锚点的旧值/新值/理由
3. （若 option-a）fmt 闸门的非恒真反证输出
4. 上面 `<outstanding_human_verification>` 两项**原样转录**进 SUMMARY 的 Outstanding Human Verification 段，供 `/gsd-verify-work` 承接
</output>
