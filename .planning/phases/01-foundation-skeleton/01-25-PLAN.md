---
phase: 01-foundation-skeleton
plan: 25
type: execute
wave: 2
depends_on: ["01-14"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-foundation-skeleton/deferred-items.md
autonomous: true
gap_closure: true
requirements: [INFRA-03]
tags: [bookkeeping, traceability, requirements, deferred-items]

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` 的 Traceability 表把 INFRA-03 映射到 Phase 4，且表下有一条注说明各 Phase 承担的部分"
    - "该注明确写出：钥匙串存取、prism-llm 唯一网络出口与唯一密钥入口、自定义 base_url 的存储与值侧校验**已在 Phase 1 完成并通过验证**；「向 Anthropic/OpenAI 兼容端点实际发请求」在 Phase 4 验收"
    - "Traceability 表里每条需求仍**只**映射一个 Phase（GSD 硬规则），且 Coverage 计数仍为 61 / 61 / 0"
    - "`deferred-items.md` 里的两条候选各有明确去向：rustfmt 一条指向 01-28 的决策 checkpoint，`[Phase ?]` 前缀一条记为顺延并写明理由"
    - statement: "本 plan 只改记账文件，不改任何产品代码、测试或脚本；`cargo test --workspace` 与 `npm run test` 不受影响"
      verification: backstop
  artifacts:
    - ".planning/REQUIREMENTS.md —— INFRA-03 的 Phase 映射与表下注"
    - ".planning/phases/01-foundation-skeleton/deferred-items.md —— 两条候选的去向记录"
  key_links:
    - "`REQUIREMENTS.md` 的 Traceability 表 → `/gsd-verify-work` 的 Requirements Coverage 与 orphan 检查"
    - "INFRA-03 的表下注 ↔ 01-14 / 01-23 / 01-25 三份 gap-closure plan 的 `requirements: [INFRA-03]` 声明（注里要点名它们**全部三份**——含本 plan 自身，使记账自洽）"
    - "INFRA-04/05 的既有先例（`REQUIREMENTS.md` 第 208 行）→ 本次加注沿用同一形态"
  prohibitions:
    - "埋点数据不得在未经用户 opt-in 的情况下离开本机"
---

<objective>
关闭 `01-VERIFICATION.md` § Requirements Coverage 与 § Gaps Summary 末段点出的记账口子，以及 `deferred-items.md` 里两条悬着的候选：

- **INFRA-03 的孤儿风险**：`.planning/REQUIREMENTS.md:148` 仍把 INFRA-03 映射到 **Phase 1**，而 Phase 1 即将收尾。它的「支持 Anthropic/OpenAI 兼容端点」分句已顺延 Phase 4（Phase 1 只有 base_url 的存储与校验面，没有 chat client；Phase 4 的 goal 明写「A3：prism-llm 传输层先行交付」）。若不动它，这条需求在 Phase 1 关闭后就没有任何 phase 负责推进——GSD 的「每条需求映射且仅映射一个 Phase」规则会把它变成孤儿。用户已拍板方案：**改映射到 Phase 4 并在表下加注**，沿用 INFRA-04/05 的既有先例。
- **`deferred-items.md` 的两条候选**：`cargo fmt --all -- --check` 全仓有差异且 CI 无 fmt 闸门（登记于 plan 01-03，建议在收尾时一次性拍板）；`state add-decision` 写出 `[Phase ?]` 前缀（属 gsd-tools 侧行为，非本仓库代码）。既然本轮的范围是「全部清干净」，两条都要有明确去向而不是继续悬着。

Purpose: 记账漂移不阻断成功标准，但它会在 Phase 1 关闭的那一刻变成一条无人认领的需求。同时，`deferred-items.md` 的存在意义是「登记在此供后续 plan 或 `/gsd-verify-work` 决定去留」——决定的时刻就是现在。

Output: 一份自洽的需求追溯表，与两条有明确去向的顺延项。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-skeleton/deferred-items.md
@.planning/phases/01-foundation-skeleton/01-VERIFICATION.md
</context>

<why_no_tracer>
缺口修补集，且本 plan 是纯记账，完全没有可执行的垂直切片；`TRACER_MODE` 不适用（理由同 01-14）。
</why_no_tracer>

<tasks>

<task type="auto">
  <name>Task 1: INFRA-03 改映射到 Phase 4 并加表下注</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` 第 87-96 行（INFRA-01 … INFRA-08 的需求正文，尤其第 91 行 INFRA-03 的三个分句）
    - `.planning/REQUIREMENTS.md` 第 140-214 行（Traceability 表全文、第 146-148 行三条 INFRA 映射、第 208 行 INFRA-04/05 的既有注、第 210-214 行 Coverage 计数）
    - `.planning/ROADMAP.md` Phase 4 段落 —— 其 goal 里「A3：prism-llm 传输层（流式/重试/keyring）先行交付」这句是本次改映射的依据，注里要引用
    - `.planning/phases/01-foundation-skeleton/01-VERIFICATION.md` § Requirements Coverage 与「INFRA-03 的顺延判断」一段 —— 里面写清了哪三部分已在 Phase 1 完成
    - `.planning/phases/01-foundation-skeleton/01-14-PLAN.md`、`01-23-PLAN.md` 与**本 plan 自身**的 frontmatter `requirements` 字段 —— 三者都声明 `[INFRA-03]`，注里要点名它们全部三份才使记账自洽
  </read_first>
  <action>
    用 `Edit` 做**局部替换**，不要 `Write` 整份文件（Traceability 表有 60+ 行，整文件写入会破坏与本次改动无关的条目）。

    第一处：把 Traceability 表里 INFRA-03 那一行的 Phase 列由 `Phase 1` 改为 `Phase 4`。Status 列保持 `Pending`（它在 Phase 4 才验收）。这一行在表中的位置可以移到 Phase 4 的其余条目附近，也可以留在原位——二选一，但若移动必须确认没有连带删掉相邻行。

    第二处：在第 208 行 INFRA-04/05 的既有注**旁边**（不是替换它）追加一条 INFRA-03 的注，形态照抄那一条。注的内容必须明确写出四件事：
    1. INFRA-03 是跨切需求，其中**钥匙串存取**（keyring-core + apple-native-keyring-store 写入并可读回）、**prism-llm 为唯一网络出口与唯一密钥入口**（`scripts/check-deps.sh` 的 single-egress / facade-egress / shell-egress 三条断言）、**自定义 base_url 的存储与值侧校验**（`crates/prism-store/src/settings.rs::validate_base_url` 的 scheme / host / userinfo / query / fragment 五项）**已在 Phase 1 完成并通过 `/gsd-verify-work` 验证**；
    2. 「代码与配置中无明文密钥」的静态检查面由 Phase 1 的 gap-closure plan 01-14 关闭（`scripts/check-secrets.sh`），密钥输入路径的归一化由 01-23 关闭——这两份 plan 的 frontmatter 因此声明 `requirements: [INFRA-03]`，与本表的 Phase 4 映射不矛盾（跨切需求自 Phase 1 起执行，验证级验收在映射的 Phase）。**本 plan（01-25）自己的 frontmatter 同样声明 `requirements: [INFRA-03]`**，理由与前两份不同：它改的正是该需求的映射记录本身，因此认领它而非实现它。注里必须把这三份 plan 都列出来，否则后续读者按注去核对时会撞见一份未被解释的声明；
    3. 剩余的「支持 Anthropic/OpenAI 兼容端点」分句（**实际向端点发出请求**）需要 Phase 4 才存在的 chat client，依据是 ROADMAP Phase 4 的 goal「A3：prism-llm 传输层（流式/重试/keyring）先行交付」；
    4. 本次改映射守住 GSD 的「每条需求映射且仅映射一个 Phase」规则。

    第三处：确认 Coverage 计数不变（v1 61 条 / mapped 61 / unmapped 0）——本次只改一条的 Phase 归属，不增不减需求条目。若计数逻辑另有依据，按实际重新核对并在 SUMMARY 里写明。

    第四处：更新文件末尾的 `*Last updated:*` 行，注明本次改动的日期与原因（Phase 1 收尾时的 INFRA-03 改映射）。
  </action>
  <verify>
    <automated>grep -n 'INFRA-03' .planning/REQUIREMENTS.md && grep -c '^| INFRA-0[1-8]' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Traceability 表里 INFRA-03 那一行的 Phase 列为 `Phase 4`
    - 表里 INFRA-03 **只出现一次**（`grep -c '^| INFRA-03 '` 输出 `1`）
    - 表里 INFRA-01 / INFRA-02 仍映射 Phase 1，INFRA-06 仍 Phase 7，INFRA-04/05/07/08 仍 Phase 8（未被连带改动）
    - 表的总行数与改动前相同：`grep -c '^| [A-Z]' .planning/REQUIREMENTS.md` 输出与改动前一致（把两次读数抄进 SUMMARY）
    - 新注含四个要点，且逐字提到 `01-14`、`01-23` 与 `01-25` **三份**声明了 `requirements: [INFRA-03]` 的 plan 编号（01-25 附带它认领该需求的理由：它改的是映射记录本身），以及 `validate_base_url`、`check-deps.sh`、`check-secrets.sh` 与 ROADMAP Phase 4 的 A3 依据
    - Coverage 三行（61 / 61 / 0）与改动前一致
    - `git diff .planning/REQUIREMENTS.md` 的改动**仅限于** INFRA-03 那一行、新增的注、以及 `Last updated` 行——不含任何其他条目（把 diff 的 stat 抄进 SUMMARY）
  </acceptance_criteria>
  <done>INFRA-03 在 Phase 1 关闭后仍有一个负责推进它的 Phase，且 Phase 1 已完成的那三部分被就地记录而不是被抹掉。</done>
</task>

<task type="auto">
  <name>Task 2: deferred-items.md 两条候选各自定案</name>
  <files>.planning/phases/01-foundation-skeleton/deferred-items.md</files>
  <read_first>
    - `.planning/phases/01-foundation-skeleton/deferred-items.md` 全文 23 行（两条候选与各自的登记理由）
    - `.planning/phases/01-foundation-skeleton/01-28-PLAN.md`（本轮同批产出，wave 6，本轮最后一份）—— rustfmt 的二选一决策 checkpoint 在那里（它的 `files_modified` 含 `rustfmt.toml`，Task 1 是本轮唯一的 `type="checkpoint:decision"`），且它的 Task 2 会回填本 Task 写下的「去向」小节；本 Task 只做指向，不做决策。**不要**写成 01-27——01-27 只做 CI 闸门接线，它自己的 objective 也写着「rustfmt 的二选一定案在 01-28」
    - `.planning/STATE.md` 第 82-138 行 —— `[Phase ?]` 前缀的实际表现（01-02 起每个 plan 追加的决策都带这个占位符，01-01 手写的四条是 `[Phase 1]`）
  </read_first>
  <action>
    在两条候选各自的条目下追加一小节「去向（Phase 1 收尾定案）」：

    第一条（`cargo fmt --all -- --check` 全仓有差异且 CI 无 fmt 闸门）：写明它已被排进本轮 gap-closure 的 **01-28-PLAN.md**，形态是一个 `checkpoint:decision`——选项 A 是「补 CI fmt 步骤 + 一次性格式化全仓」，选项 B 是「明确记为不采用 rustfmt 默认风格，不加闸门」。写清这条是 `costly` 而非 one-way（两个方向都是一次全仓 diff），以及为什么必须现在拍板：逐 plan 各自格式化会造成风格拉锯，而每多一个 phase 就多一批文件。

    第二条（`state add-decision` 写出 `[Phase ?]` 前缀）：写明它**继续顺延**，理由是它属于 gsd-tools 侧的行为、**不是本仓库的代码**——本轮的「全部清干净」范围是本仓库的代码与配置，改不了的东西不能靠本轮的计划关闭。给出顺延的具体去处：milestone 收尾时统一整理 `.planning/STATE.md` 的决策前缀，或向 gsd-tools 上游反馈。同时写明它的实际影响是**记账可读性**而非正确性（决策内容本身完整，只是 Phase 编号占位）。

    在文件顶部那段说明（「不在本 phase 修，登记在此供后续 plan 或 `/gsd-verify-work` 决定去留」）之后补一行：本文件的两条候选已于 Phase 1 收尾的 gap-closure 轮次逐条定案，去向见各条下方；本文件此后只承接**新**发现的范围外事项。
  </action>
  <verify>
    <automated>grep -c '去向（Phase 1 收尾定案）' .planning/phases/01-foundation-skeleton/deferred-items.md && grep -c '01-28-PLAN.md' .planning/phases/01-foundation-skeleton/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '去向（Phase 1 收尾定案）' .planning/phases/01-foundation-skeleton/deferred-items.md` 输出 `2`（两条候选各一节）
    - `grep -c '01-28-PLAN.md' .planning/phases/01-foundation-skeleton/deferred-items.md` 输出 `>= 1`——上一条只数小节标题，plan 编号写错时它仍输出 `2`，对「rustfmt 去向指向哪一份 plan」零判别力；本条是那件事唯一有判别力的自动化判据。它只正向断言 `01-28` 被点名，不断言别的编号不出现，因此执行者若在记录里补一句「此前误记为 01-27」也不会假红
    - 第一条的去向节里逐字出现 `01-28-PLAN.md` 与 `checkpoint:decision`，并列出选项 A / 选项 B 两种取向；该节里作为 rustfmt 决策所在地被点名的 plan 编号**只有 01-28 一个**（人工通读该节确认，并把该节全文抄进 SUMMARY）
    - 第二条的去向节里写明「属 gsd-tools 侧行为、非本仓库代码」这条顺延理由，以及具体去处（milestone 收尾整理 / 上游反馈）
    - 两条原有的登记内容（发现于 plan 01-03 / 01-05 的原文与理由）**未被删除或改写**——`git diff` 只有新增行，没有删除行（`git diff --stat` 的删除计数为 0，抄进 SUMMARY）
    - 文件顶部说明段落已补上「本文件此后只承接新发现的范围外事项」那一行
  </acceptance_criteria>
  <done>`deferred-items.md` 里不再有悬而未决的条目——一条排进了决策 checkpoint，一条有明确的顺延理由与去处。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 需求追溯表 → 后续 phase 的验收范围 | 一条无人映射的需求不会被任何 `/gsd-verify-work` 检查；孤儿需求是静默的 |
| 顺延清单 → 项目的技术债面 | 一条永远悬着的候选与一条被明确接受的取舍在文件里长得一样 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01G-41 | Repudiation | INFRA-03 在 Phase 1 关闭后成为孤儿需求 | medium | mitigate | Task 1：改映射到 Phase 4 + 表下注记录 Phase 1 已完成的部分；保持一需求一 Phase |
| T-01G-42 | Repudiation | `deferred-items.md` 的候选无限期悬置 | low | mitigate | Task 2：两条各自定案，一条指向决策 checkpoint、一条写明顺延理由与去处 |
| T-01G-43 | Tampering | 用 `Write` 覆盖 `REQUIREMENTS.md` 会丢掉表外条目 | high | mitigate | Task 1 明确要求用 `Edit` 局部替换；配「表总行数与改动前一致」的验收判据 |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | 本 plan 不安装任何包、不改任何代码 |
</threat_model>

<verification>
```bash
grep -n 'INFRA-03' .planning/REQUIREMENTS.md
grep -c '^| [A-Z]' .planning/REQUIREMENTS.md          # 表总行数应与改动前一致
git diff --stat .planning/REQUIREMENTS.md              # 改动面必须极小
git diff --stat .planning/phases/01-foundation-skeleton/deferred-items.md   # 删除计数应为 0
cargo test --workspace && npm run test -- --run        # 回归：纯记账 plan 不该影响任何测试
```
</verification>

<success_criteria>
- INFRA-03 映射到 Phase 4，且 Phase 1 已完成的三部分被表下注就地记录
- Traceability 表的其余 60 条一字未动
- `deferred-items.md` 的两条候选各有明确去向，原有登记内容完整保留
</success_criteria>

<artifacts_this_phase_produces>
| 类别 | 名称 | 位置 |
|---|---|---|
| 表行（改） | Traceability 表 INFRA-03 行的 Phase 列（`Phase 1` → `Phase 4`） | `.planning/REQUIREMENTS.md` |
| 注（新） | INFRA-03 跨切需求的表下注（沿用 INFRA-04/05 先例形态） | `.planning/REQUIREMENTS.md` |
| 小节（新） | 两条顺延候选各自的「去向（Phase 1 收尾定案）」小节 | `.planning/phases/01-foundation-skeleton/deferred-items.md` |
| 说明行（新） | 「本文件此后只承接新发现的范围外事项」 | `.planning/phases/01-foundation-skeleton/deferred-items.md` |

无新增文件、无代码改动、无新增依赖。
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-foundation-skeleton/01-25-SUMMARY.md` when done.

SUMMARY 必须包含改动前后 Traceability 表的行数读数与 `git diff --stat`，以证明改动面确实只有 INFRA-03 一行加一条注。
</output>
