---
phase: 01-foundation-skeleton
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-secrets.sh
  - crates/prism-mcp/src/deps.rs
  - crates/prism-mcp/src/middleware.rs
  - justfile
  - .github/workflows/ci.yml
autonomous: true
gap_closure: true
requirements: [INFRA-01, INFRA-03]

must_haves:
  truths:
    - "扫描器看得见 Anthropic 形态：`bash scripts/check-secrets.sh selftest` 退出 0，其中 `sk-ant-api03-…` 形态的假串被判为命中——01-VERIFICATION.md 记录的 5 行取样里那 4 行漏网的，现在一条不漏"
    - "长度阈值有成对边界样本：`sk-` 后恰好 20 个字符的样本命中，恰好 19 个字符的样本不命中——阈值不是一个没人验证过的数字"
    - "非 `sk-` 形态也覆盖：`(api_key|apikey|api-key|secret|token|password)` 后接 `=` 或 `:` 再接引号串、以及 `ghp_` / `AKIA` 前缀，各有一条命中样本"
    - "既有 fixture 仍不命中：`FIXTURE_SECRET`（prism-llm）与 `FAKE_KEY`（Settings.test.tsx）两串在 selftest 的阴性样本组里，扫描器没有为了迁就 fixture 而放宽"
    - "selftest 与 scan 共用同一个 `$PATTERN` 变量——不存在「测试的是正则的副本」这种恒真形态"
    - "扫描器不再把自己排除在外：`bash scripts/check-secrets.sh scan` 的受检集合包含 `scripts/check-secrets.sh` 本身，而它退出 0 这件事本身就证明脚本源码里没有任何命中自己正则的字面量"
    - "`docs/` 的整目录排除被取消：执行时实测确认 docs 下零命中，因此不需要任何 docs 排除；若将来某篇文档必须引用正则，只排除那一个文件路径并在脚本里写明理由"
    - "`.planning/` 仍排除，且理由写在脚本注释里：规划与验证文档按设计要引用取样密钥（01-VERIFICATION.md 就是活证据）"
    - "widen 之后全仓仍绿：`bash scripts/check-secrets.sh all` 退出 0——两处新命中（prism-mcp 的两个测试局部变量）按既有 fixture 约定改名解决，而不是放宽扫描器"
    - "闸门在两个调用点都被更新：`justfile` 与 `.github/workflows/ci.yml` 都显式跑到 selftest，否则 selftest 只是一个约定而不是闸门"
    - "非恒真反证成立：把一个 `sk-ant-api03-…` 形态的假串追加进任意受版本控制的源文件后 `bash scripts/check-secrets.sh scan` 退出 1；把它追加进 `scripts/check-secrets.sh` 自身同样退出 1"
  artifacts:
    - path: "scripts/check-secrets.sh"
      provides: "明文密钥静态检查：widen 后的 PATTERN + `scan` / `selftest` / `all` 三个子命令 + 收窄后的排除集"
      exports: ["scan", "selftest", "all"]
      min_lines: 80
    - path: "justfile"
      provides: "本机简写；check-secrets 相关 recipe 指向新的子命令形态"
      min_lines: 42
    - path: ".github/workflows/ci.yml"
      provides: "macOS CI；密钥扫描步骤跑 selftest + scan 两半"
      min_lines: 101
  key_links:
    - from: "scripts/check-secrets.sh"
      to: "scripts/check-secrets.sh"
      via: "selftest 与 scan 共用同一个 PATTERN 变量；scan 的受检集合包含脚本自身"
      pattern: "selftest"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-secrets.sh"
      via: "Plaintext secret scan 步骤调用 all（selftest 先跑，scan 后跑）"
      pattern: "check-secrets.sh all"
    - from: "justfile"
      to: "scripts/check-secrets.sh"
      via: "check-secrets / check-secrets-selftest / check-all 三条 recipe 单行委托"
      pattern: "check-secrets.sh"
  prohibitions:
    - requirement_id: INFRA-03
      category: transparency
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT 为了让某个既有 fixture、测试串或文档片段通过而放宽明文密钥扫描器（加 allowlist 条目、加整目录排除、降低长度阈值都算）。撞车时改 fixture，不改防线——这条约定已由 prism-llm 的 FIXTURE_SECRET 与前端的 FAKE_KEY 两处注释确立，本 plan 把它扩到新增的两处并用 selftest 的阴性样本组把它变成可执行断言。"
---

<objective>
关闭 01-VERIFICATION.md gap 2（Blocker）：`scripts/check-secrets.sh` 退出 0 是成功标准 4
第三分句的主要自动化证据，而它的 PATTERN 要求 `sk-` 后紧跟 ≥16 个**连续**字母数字——
Anthropic 的 `sk-ant-api03-…` 在第三个字符的连字符处就断开。verifier 用 5 行真实形态的
明文密钥取样，只命中 1 行。

Purpose: 本项目点名 Anthropic Messages API 是一等端点（CLAUDE.md），`Settings.tsx:149` 的
placeholder 就写着 `https://api.anthropic.com`。也就是说最可能被泄漏的那个供应商的密钥，
恰好是扫描器唯一看不见的。一次绿色扫描在这里等于没扫——这不是覆盖率不足，是**证据失效**。

Output: widen 后的 PATTERN（已逐条实测）+ 一个 `selftest` 子命令（阳性/阴性样本各一组，
与 scan 共用同一个正则变量）+ 收窄的排除集（去掉 docs/ 整目录、去掉脚本自排除）+
两个调用点的更新 + 两处新命中按既有 fixture 约定改名解决。

本 plan 服务的 CONTEXT 决策是 **D-05**（非密钥配置存 settings 表，密钥走钥匙串、绝不入库）的
后半句：plan 01-10 把这条边界在**写入路径**上补齐，本 plan 让**静态扫描**真的看得见它要挡的
东西。两者是同一条决策的两处失守，缺任一半这条决策都只是注释。
全 plan 集的 CONTEXT 决策账（含本轮不重新打开的六条）记在 plan 01-10 的 `<objective>` 里。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-skeleton/01-VERIFICATION.md
@.planning/phases/01-foundation-skeleton/01-PATTERNS.md
@.planning/phases/01-foundation-skeleton/01-02-SUMMARY.md
</context>

## Artifacts this phase produces

- **shell 函数**（`scripts/check-secrets.sh`）：`scan()`、`selftest()`、`main()`
- **shell 子命令**：`scan` / `selftest` / `all`（无参数默认 `all`，比旧的默认 scan-only 更严，
  因此既有的 `bash scripts/check-secrets.sh` 调用形态向后兼容且自动变强）
- **justfile recipe**：`check-secrets-selftest`（既有的 `check-secrets` / `check-all` 改为显式跑 `all`）
- **Rust 测试局部变量改名**（不是新符号，但会出现在 diff 里）：
  `crates/prism-mcp/src/deps.rs` 的 `debug_does_not_reveal_the_bearer_token` 内、
  `crates/prism-mcp/src/middleware.rs` 的 `constant_time_eq_agrees_with_equality_on_every_shape` 内

## PATTERN 的实测结果（计划阶段已跑过，执行时按此核对）

widen 后的正则对 21 条样本逐条验证过，结果如下——执行者应当在 selftest 里复现这张表：

| 样本形态 | 期望 | 说明 |
|---|---|---|
| `sk-ant-api03-` + 24 位字母数字 | 命中 | 01-VERIFICATION 取样第 1 行，旧正则漏 |
| `ANTHROPIC_API_KEY=` + `sk-ant-api03-` 串（无引号） | 命中 | 取样第 3 行，旧正则漏 |
| `Authorization: "Bearer sk-ant-api03-…"` | 命中 | 取样第 4 行，旧正则漏 |
| `sk-openai-realkeyvaluehere1234` | 命中 | 取样第 2 行，旧正则漏 |
| `sk-` + 36 位纯字母数字 | 命中 | 取样第 5 行，旧正则唯一命中的那条（回归保护） |
| `apiKey: "realkeyvaluehere1234"` | 命中 | 冒号赋值 + 大小写混排（需 `-i`） |
| `password = "hunter2hunter2"` | 命中 | password 形态 |
| `ghp_` + 28 位 / `AKIA` + 16 位 | 命中 | GitHub / AWS 前缀 |
| `sk-` + 恰好 20 字符 | 命中 | 阈值边界（下界） |
| `sk-` + 恰好 19 字符 | **不**命中 | 阈值边界（下界之下） |
| `prism-test-secret-value`（FIXTURE_SECRET 的值） | **不**命中 | 既有 fixture 必须继续通过 |
| `fixture-not-a-real-credential`（FAKE_KEY 的值） | **不**命中 | 既有 fixture 必须继续通过 |
| plan 01-10 新增的四条凭据型 URL fixture | **不**命中 | 与 01-10 的「Fixture 决定」一致 |

widen 后对全仓（除 `.planning/`）扫描，**恰好两处新命中**，都在 prism-mcp 的测试代码里，
都是「局部变量名叫 secret / token，右边跟着一个引号串」的形态——正是扫描器该抓的形状。
`docs/` 下**零命中**，所以 docs 整目录排除已无存在理由。

<tasks>

<task type="auto">
  <name>Task 1: widen PATTERN、加 selftest 子命令、收窄排除集</name>
  <reversibility rating="costly">正则本身改一行就能回退，但本 task 同时确立了一条会被后续所有 phase 继承的约定——「fixture 撞上扫描器时改 fixture，不改扫描器」。放宽它不需要迁移，却会让此后每一次「加个 allowlist 就好了」的修复看起来合理，而那正是本 gap 的成因（旧正则的窄，本质上就是一次未经权衡的放宽）。</reversibility>
  <files>scripts/check-secrets.sh</files>
  <read_first>
    - `scripts/check-secrets.sh`（被改文件，31 行全读）：13-18 的 `QUOTE` / `NOT_QUOTE` 拆串技巧
      与它上方那句注释（脚本自身不得写出成串的引号字面量）；20-23 的 `git grep … || true`
      与三条排除 pathspec
    - `scripts/check-deps.sh`（结构 analog，全读）：`set -euo pipefail`、长文件头说明「为什么这条断言
      存在 + 本文件是唯一实现，justfile 与 CI 只是调用者」、一 assertion 一个具名 `check_*` 函数、
      失败打 `FAIL: …` 到 stderr 并 `return 1`、成功打一行 `OK: …`、`main()` 按 `${1:-all}` 分派
      且 `usage:` 兜底 `exit 2`
    - `.planning/phases/01-foundation-skeleton/01-VERIFICATION.md` gap 2 的 `missing:` 四条与
      § SC-4 的 5 行取样表（本 task 的验收清单就是那张表）
    - `.planning/phases/01-foundation-skeleton/01-PATTERNS.md` § `scripts/check-secrets.sh` 一节：
      WR-11 的辨析（`check-deps.sh` 的 `|| true` 是反模式，这里的 `|| true` 是**承重**的——
      `git grep` 无命中时退出 1），以及「self-test 无 analog，机制需自造」
    - `.planning/STATE.md` § Blockers/Concerns 中「依赖方向断言用 herestring 而非管道喂 grep」
      那一条：`pipefail` + `grep -q` 早退会因 SIGPIPE 让断言静默恒绿。本 task 的 selftest
      逐条喂样本时**必须**用 herestring
    - `crates/prism-llm/src/secrets.rs` 103-106（`FIXTURE_SECRET` 及其注释）与
      `src/pages/Settings.test.tsx` 27-29（`FAKE_KEY` 及其注释）：两处 fixture 命名约定的原文，
      它们的值要进 selftest 的阴性样本组
  </read_first>
  <action>
    把 `scripts/check-secrets.sh` 重写为 `scripts/check-deps.sh` 的结构：文件头长注释说明这条
    断言为什么存在、本文件是唯一实现、justfile 与 CI 只是调用者；`set -euo pipefail`；
    两个具名函数 + `main()` 分派 `${1:-all}`，未知参数打 `usage:` 并 `exit 2`。

    **PATTERN（唯一定义，两个子命令共用同一个变量——不得复制第二份）** 由四段 alternation 组成：
    - `sk-[A-Za-z0-9_-]{20,}` —— 字符类里加入连字符与下划线是本 gap 的核心修复：
      Anthropic 的 `sk-ant-api03-…` 正是在第三个字符的连字符处被旧正则截断的。
      长度下界从 16 提到 20，因为放宽字符类后 16 会把普通标识符串扫进来
    - `ghp_[A-Za-z0-9]{20,}`
    - `AKIA[A-Z0-9]{16}`
    - `(api[_-]?key|secret|token|password)[[:space:]]*[=:][[:space:]]*${QUOTE}${NOT_QUOTE}{8,}`
      —— 在旧的 `api_key=` 形态上扩了三个关键词与冒号赋值形态

    保留 `QUOTE` / `NOT_QUOTE` 的拆串技巧并把它的适用范围写进注释：**任何新子模式都要保证脚本
    源码里不出现命中自己正则的字面量**——这是下面「取消脚本自排除」的前提。

    `git grep` 调用加 `-i`（大小写不敏感），使 `API_KEY=` / `apiKey:` 两种大小写形态都进网。
    已实测：加 `-i` 对本仓库不引入任何额外误报。

    **排除集收窄到只剩一条**：保留 `.planning/`（规划与验证文档按设计要引用取样密钥——
    01-VERIFICATION.md 本身就是活证据，这条排除的理由要写进注释）。
    去掉 `docs/` 整目录排除（执行时先实测确认 docs 下零命中；若确有命中，只排除那一个具体文件
    路径并在注释里写明是哪篇文档、为什么必须引用正则）。
    去掉脚本对自身的排除——脚本被自己扫这件事，是「源码里没有命中自己正则的字面量」这条性质
    的可执行证明，同时消掉一个「谁都能往里藏东西」的盲区。

    `git grep … || true` **保留**：`git grep` 在无命中时退出 1，吞掉它是承重的，不是 WR-11 那种
    吞掉真实失败。在它旁边写一行注释区分这两种情况，并说明 selftest 就是它的配套证明——
    一次被吞掉的调用失败会让 scan 看到空串而报 OK，而 selftest 会因为阳性样本不再命中而立刻变红。

    **`selftest()`**：两组样本，逐条用 herestring 喂给 `grep -qiE "$PATTERN"`
    （**不得用管道**——`pipefail` + `grep -q` 早退的 SIGPIPE 会让断言静默恒绿，这是本 phase
    已经踩过一次的坑）。阳性组的每一条都必须命中，阴性组的每一条都必须不命中，任一条不符即
    打 `FAIL:` 到 stderr 并 `return 1`；全过打一行 `OK:` 并报出两组各自的条数。
    样本内容照本 PLAN 上方的实测表逐条落地。

    **每条阳性样本都必须由片段拼出**，使脚本源码里不出现命中自己正则的连续字面量。做法是把
    前缀拆成变量再拼：`sk` 与随后的连字符分开写、`ghp` 与下划线分开写；`AKIA` 可以整串写
    （它后面紧跟引号，`[A-Z0-9]{16}` 匹配不上）。阴性组反而可以整串直写——它们本来就不命中，
    这正是要断言的事。写完后 `scan` 会替你验证这条约束：脚本自己也在受检集合里。

    最后在文件头补一段「样本从哪来」的溯源注释：阳性组前五条逐条对应
    01-VERIFICATION.md § SC-4 的 5 行取样表（旧正则命中 1/5，新正则必须 5/5），
    阴性组前两条是 `prism-llm` 与前端两处既有 fixture 的值。
  </action>
  <verify>
    <automated>bash scripts/check-secrets.sh selftest</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-secrets.sh selftest` 退出 0，输出一行 `OK:` 并报出阳性/阴性两组的条数
      （阳性 ≥9 条、阴性 ≥5 条）
    - `bash scripts/check-secrets.sh` 与 `bash scripts/check-secrets.sh all`
      在**本 task 结束时预期退出 1**（scan 侧还有 Task 2 要处理的两处命中）——
      这不是失败，是 widen 生效的证据；Task 2 结束时必须转为退出 0。
      本 task 的 `<done>` 以 selftest 绿为准
    - `bash scripts/check-secrets.sh nonsense-subcommand` 打印 `usage:` 到 stderr 并退出 2
    - **反证 F（selftest 非恒真，落点在阳性组）**：把 PATTERN 的 `sk-` 段临时改回
      `sk-[A-Za-z0-9]{16,}`（即恢复旧行为）后重跑 selftest，退出 1 且 `FAIL:` 指向
      Anthropic 形态的那条阳性样本；撤销后转绿
    - **反证 G（selftest 非恒真，落点在阴性组）**：把 PATTERN 临时改成一个匹配任意非空行的正则
      后重跑 selftest，退出 1 且 `FAIL:` 指向阴性组；撤销后转绿
    - **反证 H（阈值边界真的在 20）**：把长度下界临时改成 `{21,}` 后重跑 selftest，退出 1 且
      `FAIL:` 指向 20 字符那条阳性样本；改成 `{19,}` 后重跑，退出 1 且 `FAIL:` 指向 19 字符那条
      阴性样本；撤销后转绿
    - 源码断言：`scripts/check-secrets.sh` 中 `PATTERN=` 这一赋值只出现一次
      （selftest 与 scan 共用同一变量，不存在正则副本）
    - 源码断言：selftest 中喂样本的每一处都是 herestring 形式，`grep` 的左侧没有管道
    - 行为断言：`bash scripts/check-secrets.sh selftest` 的运行时长 < 5 秒（纯字符串匹配，不碰 git）
  </acceptance_criteria>
  <done>widen 后的正则在 selftest 里对 01-VERIFICATION.md 的 5 行取样全部命中（旧正则 1/5）、对既有两处 fixture 全部不命中、阈值边界有成对样本；三条反证证明 selftest 不是恒真的。此时 `scan` 侧预期仍红——那两处命中由 Task 2 处理。</done>
</task>

<task type="auto">
  <name>Task 2: 让全仓在 widen 后的扫描下重新变绿，并把 selftest 接进两个调用点</name>
  <reversibility rating="reversible">改名是两处测试内的局部绑定重命名，无跨文件引用；两个调用点各加/改一行。</reversibility>
  <files>crates/prism-mcp/src/deps.rs, crates/prism-mcp/src/middleware.rs, justfile, .github/workflows/ci.yml</files>
  <read_first>
    - `crates/prism-mcp/src/deps.rs` 87-96（`debug_does_not_reveal_the_bearer_token`：那个局部绑定
      被用于 `deps(...)` 与随后的 `rendered.contains(...)`，改名要同步这两处；51-62 的手写 `Debug`
      与 T-01-29 的脱敏契约不动）
    - `crates/prism-mcp/src/middleware.rs` 194-209（`constant_time_eq_agrees_with_equality_on_every_shape`：
      那个局部绑定在整个函数体内被多次引用；注意 208 行的 `constant_time_eq("", "")` 断言
      **本 task 不动**，它是 plan 01-12 的目标）
    - `crates/prism-llm/src/secrets.rs` 103-106 与 `src/pages/Settings.test.tsx` 27-29：
      既有 fixture 命名约定的两处原文——新名字要落在同一口径里，并各补一行同样意思的注释
    - `justfile` 23-30（`check-secrets` 与 `check-all` 两条 recipe，以及「每条只做单行委托、
      不重复实现断言逻辑」的文件头约定）
    - `.github/workflows/ci.yml` 34-35（Plaintext secret scan 步骤）与文件头「断言逻辑的唯一实现
      是 scripts/ 下的脚本」的说明
    - `.planning/phases/01-foundation-skeleton/01-PATTERNS.md` § Shared Patterns → 「CI/justfile
      dual call sites for every script」
  </read_first>
  <action>
    **两处改名（不是放宽扫描器）。** widen 后全仓恰好两处新命中，都是 prism-mcp 测试里
    「局部变量名是 secret / token，紧跟一个引号串」的形态——扫描器抓的就是这个形状，它没抓错。
    按 `FIXTURE_SECRET` / `FAKE_KEY` 已确立的约定处理：改 fixture，不改防线。

    - `crates/prism-mcp/src/deps.rs` 的 `debug_does_not_reveal_the_bearer_token` 里，把那个
      承载假 token 的局部绑定改名为 `fixture_bearer`，并同步它在 `deps(...)` 与
      `rendered.contains(...)` 两处的引用。**字面量的值不必改**——它已经是刻意混淆过的形态，
      改名后整行不再命中。
    - `crates/prism-mcp/src/middleware.rs` 的 `constant_time_eq_agrees_with_equality_on_every_shape`
      里，把那个承载正确 token 的局部绑定改名为 `configured`（语义更准：它是「配置的 token」，
      与函数签名里的 `expected` 参数同义但不同作用域），并同步函数体内所有引用。
      该函数的断言集合本 task 一条不动。
    - 两处各补一行注释，说明改名的理由与 `FIXTURE_SECRET` / `FAKE_KEY` 同源：扫描器是防线，
      不该为了迁就测试局部变量而放宽。

    **两个调用点更新（否则 selftest 只是约定，不是闸门）：**
    - `justfile`：`check-secrets` recipe 改为显式跑 `all`；新增 `check-secrets-selftest` recipe
      单行委托到 `selftest`；`check-all` 里的密钥检查那一行同样改为显式 `all`。
      每条 recipe 上方的注释更新，说明 `all` 现在是 selftest + scan 两半。
    - `.github/workflows/ci.yml`：`Plaintext secret scan` 步骤改为显式跑 `all`，并把步骤名改成
      能反映两半的措辞。理由写进步骤旁的注释：无参数默认值虽然也是 `all`，但闸门不该依赖默认值——
      默认值哪天改回 scan-only，CI 会静默失去 selftest 这一半而照常绿。

    执行顺序建议：先改名再跑 `bash scripts/check-secrets.sh all`，确认从 Task 1 结束时的退出 1
    变成退出 0；这个「红 → 绿」的转变本身就是 widen 真的生效过的证据，请记进 SUMMARY。
  </action>
  <verify>
    <automated>bash scripts/check-secrets.sh all &amp;&amp; cargo test -p prism-mcp &amp;&amp; cargo clippy -p prism-mcp -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-secrets.sh all` 退出 0（selftest 与 scan 两半都绿）
    - `bash scripts/check-secrets.sh scan` 退出 0，且它的受检集合包含 `scripts/check-secrets.sh`
      本身（排除 pathspec 只剩 `.planning/` 一条）
    - `cargo test -p prism-mcp` 退出 0，且 `debug_does_not_reveal_the_bearer_token` 与
      `constant_time_eq_agrees_with_equality_on_every_shape` 两个测试仍在、仍绿、断言条数不变
    - `cargo clippy -p prism-mcp -- -D warnings` 退出 0
    - `cargo test --workspace` 退出 0（改名没有牵动别处）
    - **反证 I（scan 非恒真，普通源文件）**：往任意一个受版本控制的 `.rs` 或 `.ts` 文件里临时追加
      一行含 `sk-ant-api03-` 形态假串的赋值，`bash scripts/check-secrets.sh scan` 退出 1 并把该行
      打到 stderr；删掉后转绿。**这条正是 01-VERIFICATION.md 判定「绿色扫描等于没扫」的那个
      场景——它现在必须变红**
    - **反证 J（脚本自身不再是盲区）**：把同样形态的假串临时追加进 `scripts/check-secrets.sh`
      自身，`bash scripts/check-secrets.sh scan` 同样退出 1；删掉后转绿
    - **反证 K（调用点真的被更新了）**：把 `scripts/check-secrets.sh` 的 selftest 临时改成必定失败
      （例如往阳性组塞一条不可能命中的样本），`just check-all`（若本机装了 just）与
      CI 的 Plaintext secret 步骤所跑的那条命令都退出非 0；撤销后转绿
    - 行为断言：`justfile` 里与 `scripts/check-secrets.sh` 相关的每条 recipe 都是单行委托，
      没有把断言逻辑复制第二份
  </acceptance_criteria>
  <done>widen 后的扫描器在全仓退出 0，两处新命中按既有 fixture 约定改名解决（防线一寸没放宽），selftest 在 justfile 与 CI 两个调用点都成为闸门，且三条反证证明这条闸门不是恒真的。01-VERIFICATION.md gap 2 关闭。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 开发者/agent 的编辑 → git 仓库 → 远端与构建产物 | 提交进仓库的密钥会随源码与构建产物一并流出，且历史不可撤销 |
| CI 的绿灯 → 「无明文密钥」这条断言 | 一次绿色扫描被当作成功标准 4 第三分句的证据；扫描器失明时这条证据链是断的 |
| 扫描器自身的源文件 → 受检集合 | 一个把自己排除在外的检查器，其自身就是最容易被利用的藏匿点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-44 | Information Disclosure | `scripts/check-secrets.sh` 的 PATTERN 对 `sk-ant-api03-…`（本项目一等端点的密钥格式）完全失明 | **high** | mitigate | 字符类扩到 `[A-Za-z0-9_-]`、长度下界提到 20；selftest 用 01-VERIFICATION.md 的 5 行取样做阳性组，命中率必须 5/5（旧正则 1/5） |
| T-01-45 | Repudiation | 检测控件失效但仍报绿——「扫过了」这条记录与事实不符，且失效本身无人察觉 | **high** | mitigate | selftest 让正则的判别力成为每次 CI 都执行的断言；阳性/阴性两组各自的反证写进验收项；selftest 与 scan 共用同一个 `$PATTERN` 变量，杜绝「测的是副本」 |
| T-01-46 | Tampering | 扫描器把自身排除在受检集合外，成为一个人人可写的盲区 | medium | mitigate | 取消自排除；阳性样本一律由片段拼出，使脚本源码不含命中自身正则的字面量——`scan` 退出 0 即为该性质的可执行证明 |
| T-01-47 | Tampering | 后续为迁就某个 fixture 而放宽正则/加 allowlist，静默地把检测面重新缩回去 | medium | mitigate | 阴性样本组把两处既有 fixture 的值钉成断言；本 plan 一条 allowlist 都不新增，两处新命中以改名解决；prohibition 显式登记该 must-NOT |
| T-01-48 | Information Disclosure | `docs/` 整目录排除使文档里的明文密钥永不可见 | low | mitigate | 实测确认 docs 下零命中后取消整目录排除；将来确需引用正则的文档只排除该单个文件路径并注明理由 |
| T-01-49 | Repudiation | `git grep … \|\| true` 吞掉调用失败，后续判空即报 OK（WR-11 的同形态） | medium | accept + compensate | 这里的 `\|\| true` 是承重的（`git grep` 无命中即退出 1），不能删；补偿控件是 selftest——一次被吞掉的调用失败会让阳性样本不再命中而立刻变红 |
</threat_model>

<verification>
- `bash scripts/check-secrets.sh selftest` / `scan` / `all` 三条各自退出 0
- `bash scripts/check-secrets.sh nonsense` 退出 2 并打 usage
- 六条反证（F 阳性组、G 阴性组、H 阈值上下界、I 普通源文件注入、J 脚本自身注入、K 调用点）
  各自使对应命令退出非 0，且失败落点是被守的那条样本而非前置条件
- `cargo test --workspace` / `cargo clippy -p prism-mcp -- -D warnings` 退出 0
- `bash scripts/check-deps.sh all` 退出 0（本 plan 不动依赖图）
- 与 01-VERIFICATION.md § SC-4 的 5 行取样表逐行对照：命中数从 1/5 变成 5/5
</verification>

<success_criteria>
成功标准 4 第三分句的**执行机制**恢复有效：一次绿色的 `check-secrets.sh` 现在真的意味着
「受版本控制的文件里没有本项目最可能出现的那几种明文密钥」，而这句话本身由 selftest
每次执行时重新证明一遍。INFRA-03 的证据侧解除阻塞。
</success_criteria>

<output>
Create `.planning/phases/01-foundation-skeleton/01-11-SUMMARY.md` when done
</output>
